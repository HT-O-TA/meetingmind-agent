
---

## MeetingMind 面试问答 - 中小厂/初创公司版

> 注：此版本重点突出实用性、落地能力和业务价值，减少深度技术细节，适合快速面试准备。

---

## RAG 板块问题清单（共 20 问）

### 一、核心功能与选型（6问）

1. 这个项目是做什么的？解决什么问题？
A: MeetingMind 是一个基于 RAG + Agent 的会议智能助手，解决会议记录难检索、会议内容遗忘、待办事项流失等问题。

2. 你为什么选择 PostgreSQL 存向量，不用专门的向量库？
A: 初期数据量小（50篇文档），PostgreSQL 完全够用，避免引入额外依赖，降低运维复杂度。未来数据量大了再迁 PGVector 或 Milvus。

3. Embedding 模型选的是什么？为什么？
A: 用的 bge-m3，原因：支持中英双语，性能好，轻量。从 MiniLM 切换过来后，平均相似度从 0.725 提升到 0.79，召回率从 78.6% 到 85.6%。

4. 你们的检索流程是怎样的？
A: 向量检索（余弦相似度）+ BM25关键词检索融合 → 按 BM25×0.3 + 向量×0.7加权合并 → BGE-Reranker 精排 → 返回 top_k。

5. 切分策略有几种？为什么这么设计？
A: Speaker模式（按发言者切分，适合会议）和 fixed/paragraph 等，针对会议场景优化，召回率最高 86.1%。

6. 支持流式输出吗？怎么实现的？
A: 支持，用 SSE（Server-Sent Events）。后端用 StreamingResponse，前端用 Fetch API 逐字渲染。

### 二、效果与优化（8问）

7. 你们的效果怎么样？有数据吗？
A: 召回率在 60%-86% 之间，平均相似度 0.79，MRR 0.75，命中率 43%-63%。

8. 你做过哪些优化？效果如何？
A: ① Embedding 升级，召回 +7%；② Speaker 短发言合并，从 59.6% 恢复到 86.1%；③ 多路召回融合，命中率 +5%；④ 重排序，相似度 +3%。

9. 遇到过什么 Bad Case？怎么解决的？
A: 最典型的是 Speaker 模式下单个发言太短（只有"好的"两字），无法检索，加了短发言合并策略后解决。

10. 有评估体系吗？
A: 有，233正例+30负例数据集，用 RAGAS 指标（Faithfulness、Relevance 等），还有快速/深度双模式。

11. 怎么控制 LLM 成本？
A: max_tokens 限制（1000），评估用便宜的 qwen-turbo，Context 压缩只送 top 相关。

12. LLM 调用失败怎么处理？
A: 多级降级：超时 → 流式转非流式 → 检索结果兜底，还有日志和监控。

13. 怎么减少幻觉？
A: Prompt 明确要求"没找到就说不知道"，结合检索约束，把相似度传给 LLM 参考。

14. 支持多轮对话吗？
A: 当前是单轮，简化复杂度，因为会议场景检索为主，未来可以加。

### 三、亮点总结（6问）

15. 你的项目和其他开源 RAG 有什么不同？
A: 聚焦会议场景，按说话人切片，集成 Agent 做任务规划，还有完整的评估体系。

16. 你觉得做的最好的部分是什么？
A: 评估体系完整（可量化、可回归），多路召回+重排序效果好。

17. 最大的技术难点是什么？
A: Speaker 模式下碎片化发言导致召回差，通过短发言合并解决，Recall 从 59.6% 到 86.1%。

18. 这个项目怎么落地到实际业务？
A: 可以对接企业内部会议系统，上传会议记录自动向量化，员工可以快速检索，Agent 可以自动生成纪要和待办。

19. 你在项目中主要负责哪部分？
A: （根据实际情况回答，例如：全栈开发，从前端 Vue 到后端 FastAPI，RAG 系统和 Agent 系统都参与了）

20. 这个项目中 RAG 板块的亮点有哪些？
A: ① 多路召回+重排序，命中率 +5%；② Speaker 模式，按发言者切分，适合会议；③ 完整评估体系，数据驱动迭代；④ 异步全栈，性能好。

---

## Agent 决策与工具调用板块（共 10 问）

### 一、核心设计（5问）

21. Agent 用的什么架构？
A: LangGraph 的 Plan-Execute-Reflect，比 ReAct 更可控，支持任务拆解和并行。

22. Agent 能做什么？
A: 会议检索、待办抽取、会议纪要生成、争议点检测、问答。

23. 记忆系统怎么做的？
A: 短期（会话内）+ 长期（跨会话），当前是进程内，未来持久化。

24. 工具怎么注册和管理？
A: ToolRegistry 统一管理，继承 BaseTool，5个工具，支持 OpenAI 格式。

25. 流式输出怎么展示？
A: SSE 实时推送 Plan→Execute→Reflect 全过程，前端逐字显示。

### 二、亮点总结（5问）

26. Agent 怎么防止死循环？
A: 最大迭代次数限制 + 错误恢复系统 + 计划验证和自动修复。

27. 有任务模板吗？
A: 有，预设常见任务，相似度匹配快速定位，减少 LLM 调用。

28. Agent 怎么处理复杂任务？
A: Plan 阶段拆解，依赖分析，无依赖并行执行，有依赖顺序执行。

29. 人机确认有什么用？
A: 敏感操作（如删除）等用户确认，增加安全性。

30. Agent 板块的亮点有哪些？
A: ① Plan-Execute-Reflect 流程，可控；② 5个工具，覆盖会议场景；③ 错误恢复机制完善；④ 流式输出，体验好。

---

## 系统架构与工程实现板块（共 8 问）

### 一、架构选型（4问）

31. 整体架构是什么样的？
A: 前后端分离，Vue 3 + FastAPI + PostgreSQL，分层架构（API→服务→数据）。

32. 为什么选 FastAPI？
A: 异步支持好，自动生成 API 文档，类型安全（Pydantic），性能好。

33. 数据库表设计有哪些？
A: users、meetings、speech_records、documents、todo_items、vector_chunks，6张核心表。

34. 怎么保证 API 稳定？
A: 全局异常处理，loguru 日志按日滚动，健康检查接口，访问日志中间件。

### 二、亮点总结（4问）

35. 工程化方面做得怎么样？
A: 分层架构，类型注解，单元测试，配置中心，日志监控，齐全。

36. 用了什么设计模式？
A: 依赖注入、工厂模式、策略模式、装饰器模式、单一职责、开闭原则。

37. 如何保证安全？
A: JWT 认证，权限控制，CORS 配置，输入验证，日志审计，速率限制。

38. 架构板块的亮点有哪些？
A: ① 前后端分离，异步全栈；② PostgreSQL 多用（关系+向量+搜索）；③ 全局异常处理，统一响应；④ 缓存预留，未来可加。

---

## 大模型调用与优化板块（共 7 问）

### 一、模型与Prompt（4问）

39. 用的什么大模型？为什么？
A: 阿里云通义千问 qwen3.6-plus，原因：成本可控，中文好，速度快，OpenAI 兼容。

40. Prompt 怎么设计的？
A: 双层结构：系统提示设定角色约束，用户消息包含问题和上下文。还支持自适应 Prompt，根据 Query 类型选模板。

41. 怎么让 LLM 输出 JSON？
A: Prompt 明确要求格式，给 schema，正则提取；或者用 OpenAI 的 response_format 参数。

42. 自适应 Prompt 是什么？
A: 根据 Query 类型（事实/总结/对比/推理）自动选对应的 Prompt 模板，效果更好。

### 二、亮点总结（3问）

43. 怎么处理上下文限制？
A: 逐个加 chunk 直到字符限制，至少保留 100 字符，超出丢弃，预留压缩接口。

44. 大模型调用失败怎么办？
A: 超时返回友好提示，流式失败转非流式，检索结果兜底，可切换厂商。

45. LLM 板块的亮点有哪些？
A: ① 性价比高，qwen 模型；② 自适应 Prompt，类型路由；③ 多级降级，稳定；④ 流式输出，体验好。

---

## 评估与迭代板块（共 5 问）

### 一、评估体系（3问）

46. 怎么评估效果？
A: 233正例+30负例数据集，RAGAS 6项指标，快速模式跳过 LLM 只算检索，深度模式全算。

47. 有 A/B 测试吗？
A: 没线上，但评估模式下对比切分、top_k、embedding、是否重排，结果可导出。

48. 怎么迭代优化？
A: 根据指标：Recall 低优化检索，Precision 低优化重排，相似度低优化 Prompt。

### 二、亮点总结（2问）

49. 评估数据是怎么来的？
A: 人工设要求 → LLM 批量生成 → 人工筛选调整，半自动构建，质量和效率兼顾。

50. 评估板块的亮点有哪些？
A: ① RAGAS 集成，6大指标；② 快速/深度双模式；③ 半自动数据集，22%干扰文档；④ 回归测试预留，防止回退。

---

## 项目经验与个人总结板块（共 5 问）

51. 你从这个项目中学到了什么？
A: 技术：RAG 是全链路优化，不是只有向量检索；Agent 需要完善的错误处理；评估体系非常重要。工程：分层架构、异步、日志监控是生产必备。

52. 如果让你重新做，你会改进哪里？
A: Agent 记忆持久化，表格处理完善，用户反馈收集，知识图谱打通，前端完善。

53. 项目有什么不足？
A: 前端部分是演示，记忆没持久化，表格处理待完善，没用户反馈，知识图谱没打通。（诚实回答，然后说改进方向）

54. 面试时怎么展示这个项目？
A: ① 先讲架构图，全局观；② 演示 RAG 问答和 Agent 任务规划；③ 展示评估数据和优化过程；④ 讲 Bad Case 解决故事；⑤ 强调工程化，不是只有 demo。

55. 整个项目的亮点总结？
A: 聚焦会议场景，RAG+Agent 结合，完整评估体系，工程化落地，有数据有效果，适合中小厂快速落地。

---

## 中小厂面试重点总结

面试官最可能问的 10 个问题：
1. 这个项目解决什么问题？
2. RAG 全流程讲一下？
3. 为什么选这些技术栈？
4. 效果怎么样？有数据吗？
5. 遇到过什么难点？怎么解决的？
6. 你做了哪些优化？
7. 工程化方面有什么亮点？
8. 怎么保证系统稳定？
9. 你在里面负责什么？
10. 跟其他项目比有什么不同？

面试展示材料建议：
1. 一页架构图
2. 一页核心效果数据
3. 1 个 Bad Case 解决前后对比
4. 一个 2 分钟的功能演示视频（可选）

中小厂更看重：**落地能力、问题解决能力、快速上手能力、工程化能力**，不需要讲太深的算法细节，重点讲清楚**做了什么、为什么这么做、效果怎么样、遇到问题怎么解决的**。
