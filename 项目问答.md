
---

## RAG 板块问题清单（共 40 问）

### 一、文档处理与切片（6问）

1. 文档来源是什么格式？除了会议记录，还有其他类型的文档吗？如何处理不同格式？特殊内容例如表格是怎么处理的？
A:支持格式 ：txt、md、pdf、docx、csv、xlsx、xlsm，其中 doc 格式暂不支持。 处理逻辑 ：通过文件扩展名分发到对应解析器——文本格式直接解码 UTF-8，PDF 使用 pdfplumber（优先）或 pypdf 提取文本和表格，DOCX/XLSX 解析段落和表格内容，CSV/XLSX 将数据直接转为表格。 表格处理 ：所有格式的表格统一转为 Markdown 表格格式存储，便于后续检索时识别表格边界。最终解析结果以纯文本/Markdown 形式存入 content 字段，进入向量化和检索流程。

2. 你提到按说话人（speaker）模式切分，具体如何判断一段文本属于哪个说话人？如果会议记录中说话人标签不规范（如没有时间戳或名字缺失），你的系统如何处理？
A:人为设置五种说话人模式，用正则表达式逐行按顺序匹配判断。如果标签不规范，将缺失字段设为none，降级处理，不会影响发言内容解析。

3. 固定长度切分（fixed）的 `CHUNK_SIZE=512` 和 `CHUNK_OVERLAP=64` 是如何确定的？有没有做过实验对比不同参数的效果？
A:512和64的大小通常是默认配置。不过根据经验，512 在会议场景下能平衡语义完整性和检索粒度。未来计划用评估数据集对不同 chunk size 做 A/B 测试。

4. 对于按说话人切分，切片后的每个 chunk 保留了哪些元数据（如会议 ID、发言人、时间戳）？这些元数据在检索时如何被利用？
A:保留说话人姓名，时间偏移发言序号，关联会议ID，关联文档ID，部门信息，以及额外元数据。利用原数据可以进行精确过滤。

5. 语义分块时你调用了哪个大模型？如果 LLM 返回的语义边界有误，你的系统如何兜底？
A：使用阿里云通义千问 qwen3.6-plus 模型，通过 OpenAI 兼容接口调用。自动回退到规则分块，保证文档处理的可用性。

6. 混合策略中“段落”和“语义”是如何结合的
A:先用正则表达式按段落分割文档，基于jieba分词和停用词过滤，计算相邻段落的词频余弦相似度，当相邻段落相似度 ≥ 0.7（ semantic_threshold ）且总大小 ≤ max_chunk_size （1000字符）时合并；否则在语义边界处切分。

### 二、向量化与 Embedding（4问）

7. 你使用哪个 Embedding 模型？模型的维度是多少？为什么选择这个模型而不是其他（如 BGE、M3E、OpenAI Ada）？
A:一开始使用的是MiniLM后来换成了bge-m3,维度是1024。因为考虑到会议文本当中需要支持中英文，同时性能很好，轻量。平均相似度+7.5%到0.8左右，上下文相关性+29.3%到0.66。

8. Embedding 过程是同步还是异步的？如果有大量文档需要向量化，如何优化性能？
A:全程使用异步。效率高如果有大量文档需要向量化，采用批量向量化

9. 你是否对 Embedding 结果做了缓存？缓存策略是怎样的（例如基于文档内容的哈希）？
A:目前由于数据量小，Embedding 缓存没有实际启用。但我在设计中预留了缓存层，key 可以是文档内容的哈希值。如果未来数据量增大，可以快速接入 Redis。

10. 你是怎么选择向量索引类型？HNSW和IVF各有什么特点？
A:当前项目未使用向量索引 ，采用全量扫描模式.HNSW特点:图搜索速度快，内存占用高，适合高精度场景;IVF特点:聚类+倒排，构建快，适合大数据量

### 三、向量存储与检索（5问）

11. 你为什么选择用 PostgreSQL 的 JSON/ARRAY 存储向量，而不是专门的向量数据库（如 Milvus、Qdrant、Chroma）？这种方案的优缺点是什么？
A:项目初期数据量小（50篇文档，向量数约几百到几千），PostgreSQL 完全够用;避免引入额外依赖（Milvus/Qdrant 需要单独部署运维），降低项目复杂度

12. 检索时使用的相似度度量是什么（余弦相似度、欧氏距离、点积）？为什么？
A:query 通常短，document 长，余弦相似度不受长度影响;关注"方向"而非"长度"，适合判断主题相关性;BGE等模型推荐使用

13. 你的检索流程是：先向量检索，再 BM25，再 rerank。具体每一步的输入输出是什么？最终如何合并多路召回的结果？
A:首先向量检索接收用户query，通过余弦相似度计算返回top_k×2条结果；同时 BM25检索 基于词频和逆文档频率返回top_k×2条结果；两路结果各自归一化后，对同一文档的分数按BM25×0.3+向量×0.7加权合并，按综合分数降序取前rerank_top_n条进入重排序阶段，由BGE-Reranker交叉编码器对query与候选文档进行精排，最终输出top_k条高相关性结果。权重在后续中可以通过数据实验进行调整。

14. BM25 检索是基于什么字段进行的？是基于原始文档文本还是基于 chunk 文本？BM25 的实现是自己写的还是用了现成库（如 rank_bm25）？
A:基于 chunk 文本，自实现的BM25通过正则分词+停用词过滤计算词频(TF)和逆文档频率(IDF)

15. 如果检索结果的相关性都低于 `SIMILARITY_THRESHOLD=0.7`，你的系统会怎么处理？直接告诉用户没找到答案，还是仍然交给 LLM？
A:即使相关性不是很高，也倾向于让 LLM 判断是否可用，因为 LLM 有一定的容错和推理能力。但我们会把相似度分数传给 LLM 作为参考，让模型知道这些 chunks 可能不完全相关。

### 四、RAG 生成（4问）

16. 你的 RAG 生成 prompt 是如何设计的？包含哪些部分（指令、检索到的 chunks、对话历史、用户问题）？
A:项目RAG生成prompt采用 "系统提示+用户消息" 的双层结构设计。系统提示设定角色为"MeetingMind智能助手"，明确要求"坦诚告知用户无法回答，不编造信息"，并强调"回答要简洁明了，重点突出"。用户消息包含三部分：用户原始问题、"参考信息："标签以及按顺序编号的检索片段。上下文部分做了字符数限制，系统逐个添加chunk直到达到上限，超出部分截断丢弃。 当前实现未包含对话历史 ，每次问答都是独立的单轮对话。

17. 你如何处理检索到的 chunks 长度超过 LLM 上下文窗口的情况？是否做了截断或压缩？
A:系统做了多层上下文管理来处理 chunk 过长的问题,逐个添加 chunk，直到达到字符数限制,最后一个 chunk 可以截断，但至少保留100字符，超出的 chunk 直接丢弃

18.  你的 RAG 系统是否支持流式输出？如何实现？
A：当前RAG系统 支持流式输出 ，采用 Server-Sent Events (SSE) 实现。实现流程为：API层创建 StreamingResponse ，RAG服务层调用 ask_stream() 方法，先执行向量检索获取chunks，再构建prompt，最后通过LLM服务层的 stream() 方法调用OpenAI流式接口，逐块yield给前端。

19. Query改写实现过程是怎样的
A:项目Query改写采用 多策略融合 设计，包含Query分解、HyDE假设文档生成和Query扩展三个核心模块。 Query分解器 先通过关键词（"和"、"比较"、"哪些"等）判断问题复杂度，简单查询直接返回，中等/复杂查询调用LLM将问题拆分为可独立回答的子问题。 HyDE生成器 让LLM生成若干假设性详细回答作为检索目标，通过"以文找文"方式提升检索召回率。 Query扩展器 并行生成同义词和相關术语扩展。 Query优化器 作为调度层，支持按配置开关各模块，最终通过 get_search_queries() 汇总原始查询、子查询、假设文档、同义词和相关词形成多个检索query列表。 

### 五、评估体系（5问）

20. 你的评估数据集包含 233 个正例和 30 个负例，这些问题是人工构造的还是自动生成的？如何保证问题的覆盖度和质量？
A:由人工设置好数据集要求后，使用AI生成，并经过人工筛选和调整，属于半自动

21. 评估时，`EVAL_SKIP_LLM=True` 只计算检索指标。但你也有 Answer Similarity 指标，那是如何计算的？用了什么模型或方法计算相似度？
A:快速模式：用 BGE-M3 计算答案向量与标准答案向量的余弦相似度。深度模式：用 qwen 判断答案是否与标准答案语义一致（输出 0-1 分数或分类）

22. Recall@5、Precision@5、MRR 分别在你的评估集上达到了多少数值？有没有按问题类型（事实型、推理型等）或难度（easy/medium/hard）分别统计？
A:当前评估未按问题类型或难度分层统计,在不同切分模式下，召回率在 60%-86% 之间波动，平均相似度在0.6-0.8之间波动，MRR在0.6-0.8之间波动，命中率在0.43-0.63之间波动。

23. 负例问题（没有答案的问题）在评估中如何体现？你期望系统对负例问题返回什么？如何量化？
A:我计划增加一个'拒绝率'指标，即系统正确拒绝无答案问题的比例。目前通过人工抽检，拒答准确率大约在 80%。

24. 你提到干扰文档占比 22%，这些干扰文档是故意加入的吗？评估时是否包含了这些干扰文档？
A:是有意加入的，是在生成数据集时的要求，评估时包括这些干扰文档

### 六、优化与迭代（4问）

25. 你做过哪些具体的 RAG 优化实验？例如调整 chunk size、切换 embedding、增加 rerank 等，效果分别如何？
A: 已进行多项优化实验：① Embedding模型切换 ：从 MiniLM 切换到 bge-m3，平均相似度从 0.725 提升到 0.7918，召回率从 78.6% 提升到 85.6%；② Chunk size 调整 ：测试过 256/512/1024 三种配置，512 在召回率和相似度之间取得最优平衡；③ Speaker模式优化 ：新增短发言合并策略（ SPEAKER_MIN_CHUNK_SIZE=100 ），召回率从 59.6% 恢复至 86.1%；④ 多路召回融合 ：引入 BM25 + 向量检索融合，命中率提升约 5%；⑤ Rerank集成 ：添加 BGE-Reranker 精排，答案相似度提升约 3%。

26. 有没有遇到过典型的 bad case（检索错误或生成错误）？你是如何诊断和修复的？
A: 遇到过多个典型问题：① 碎片化发言问题 ：Speaker模式下单个chunk仅11字，导致召回率骤降（59.6%），通过 _merge_short_speeches 合并短发言修复；② Overlap过大问题 ：相邻chunk重复内容过多，通过在overlap回溯中增加heading硬隔离修复；③ 表格处理问题 ：表格被当作普通文本切分，计划通过表格识别逻辑优化；④ 语义边界错误 ：LLM返回的分块边界不合理，通过自动回退到规则分块处理。诊断方式主要通过评估指标分析、日志追踪、样本对比。

27. 你的系统是否支持用户反馈（如点赞/点踩）来持续改进？如果支持，如何利用这些反馈？
A: 可以说"当前系统还未收集用户反馈，但未来计划通过记录 chunks 和用户点击等行为进行优化。

28. 你认为当前 RAG 系统最大的瓶颈在哪里？下一步最想改进什么？
A: 当前最大瓶颈在于 检索质量与chunk粒度的平衡 ：Speaker模式下chunk偏小导致召回率波动，fixed模式下chunk偏大导致相似度下降。下一步计划：① 优化Speaker合并策略 ：动态调整 SPEAKER_MIN_CHUNK_SIZE 参数，测试 200-300 字范围；② 引入动态chunking ：根据文档类型自动选择切分策略；③ 增强表格处理 ：识别表格结构并保留结构化信息向量化；④ 完善负例处理 ：增加专门的负例评估指标（如拒绝率）；⑤ 优化prompt工程 ：增强引用来源标注要求，提升答案可追溯性。

### 七、Query优化与扩展（4问）

29. Query分解和Query扩展有什么区别？
A: Query分解（Decomposition）是将复杂问题拆解为多个简单的子问题，每个子问题可独立检索回答，如"项目A和项目B的区别"分解为"项目A是什么"和"项目B是什么"。Query扩展（Expansion）是在原始查询基础上增加同义词、相关术语或改写表达，以提升检索召回率，如"预算"扩展为"预算、费用、花销、资金"。分解解决"多维度"问题，扩展解决"表达多样性"问题。项目Query优化器同时包含这两个模块，Query分解器处理复杂多维度问题，Query扩展器处理表达多样性。

30. 你用到知识图谱索引了没？相比纯向量索引有哪些优势？
A: 项目已实现知识图谱模块（knowledge_graph.py），支持实体抽取、关系抽取和图谱构建。知识图谱优势：① 结构化推理能力 ：支持多跳查询，如"谁参与了张三负责的项目"；② 可解释性强 ：关系路径清晰可见；③ 精准检索 ：基于实体和关系而非语义相似度；④ 处理模糊查询 ：通过实体消歧和关系推理解决同义词问题。纯向量索引优势在于语义理解能力强，但缺乏结构化推理能力。知识图谱劣势是构建成本高，需要额外的NER和关系抽取模型。知识图谱模块已实现，但尚未与主 RAG 检索链路打通。目前作为独立的实验性模块，未来可以用于增强多跳查询。

31. 如何实现实体识别和关系抽取？
A: 项目EntityExtractor采用 规则+LLM混合方案 ：规则层使用正则表达式匹配已知模式（如"项目[A-Z0-9]+"、人名+职位后缀），快速抽取高频实体；LLM层调用通义千问进行深度抽取，支持PERSON、ORGANIZATION、PROJECT等8种实体类型。RelationExtractor同样采用规则+LLM混合：规则层匹配"X参与Y"、"X负责Y"等模式；LLM层基于抽取的实体列表，生成实体间的语义关系（如"汇报给"、"依赖"）。两者都通过source_chunks追踪实体/关系来源，便于后续溯源。

32. 什么是自适应Prompt？如何根据Query类型选择Prompt？你用到了吗？
A: 自适应Prompt是根据用户问题类型动态选择最优Prompt模板的机制，避免"一刀切"导致的效果折损。项目AdaptivePromptBuilder实现了8种Query类型分类：事实型（FACTUAL）、总结型（SUMMARY）、对比型（COMPARISON）、推理型（REASONING）、列表型（LIST）等，每种类型有独立的system_prompt和user_template。如对比型启用CoT（Chain-of-Thought）推理并要求"突出差异点"；推理型要求"列出已知信息、展示推理步骤"；总结型要求"结构化组织信息、包含主题决议待办"。Query分类器基于关键词匹配+LLM双重判断，根据类型路由到对应模板。

### 八、上下文压缩与评估（3问）

33. 你有没有用到上下文压缩？为什么要使用？
A: 项目实现了ContextCompressor上下文压缩模块，原因：① LLM上下文窗口有限 ：通义千问等模型有token上限，不能无限塞入检索结果；② 减少噪声干扰 ：检索到的chunks可能包含大量无关内容，压缩可提升答案质量；③ 降低延迟和成本 ：token数直接影响API调用费用和响应时间。压缩策略包括：按比例压缩（保留top N结果）、按相关性压缩（分数低于阈值则丢弃）、基于token数的硬截断（逐个添加chunk直到达到max_tokens限制）。在AdaptivePromptBuilder中，压缩后的上下文会标注来源编号，便于答案溯源。

34. RAGAS是什么？有没有用到？
A: RAGAS（Retrieval-Augmented Generation Assessment）是一套专门评估RAG系统效果的指标框架，包含6大核心指标：Faithfulness（答案忠实度）、Answer Relevancy（答案相关性）、Context Precision（上下文精确度）、Context Recall（上下文召回率）、Answer Similarity（答案相似度）、Answer Correctness（答案正确性）。项目RAGASEvaluator已集成RAGAS评估，支持单次评估和批量评估两种模式。单次评估调用evaluate()方法返回完整指标，批量评估通过batch_evaluate()汇总所有样本的指标均值。快速模式（EVAL_SKIP_LLM=True）只计算检索指标，跳过LLM调用以提升评估速度。

35. 项目中Query改写的具体实现细节是什么？HyDE假设文档生成是如何工作的？
A: Query改写采用 四模块协作架构 ：QueryDecomposer分析问题复杂度，简单查询直接返回，中等/复杂问题调用LLM将问题拆分为可独立回答的子问题；HyDEGenerator让LLM生成2-3个假设性的详细回答作为"伪文档"，通过"以文找文"方式提升检索召回率，因为假设文档的embedding可能与真实文档更接近；QueryExpander并行生成同义词和相关术语扩展查询表达；QueryOptimizer作为调度层，支持按配置开关各模块，最终通过get_search_queries()汇总原始查询、子查询、假设文档、同义词形成多个检索query列表。这种多策略融合确保了不同类型问题都能获得最佳检索效果。

### 九、高级特性与未来规划（4问）

36. 项目如何处理多轮对话？是否支持对话历史？
A: 当前RAG系统是 单轮问答模式 ，每次问答都是独立的，Prompt中不包含对话历史。设计考量：会议记录场景以信息检索为主，单轮问答已能满足大部分需求；简化系统复杂度，避免历史上下文累积导致的prompt膨胀问题。未来规划支持多轮对话模式，在Prompt中追加[历史问题]和[历史答案]标签，LLM可基于前几轮对话上下文进行追问和澄清，类似ChatGPT的conversation模式。

37. 你如何评估生成答案的质量？除了自动指标，有没有人工事后抽检？
A: 自动评估采用多层次指标：检索层用Recall@K、Precision@K、MRR；生成层用RAGAS六项指标；端到端用Answer Similarity（快速模式用BGE-M3向量相似度，深度模式用LLM判断）。人工事后抽检通过日志记录实现：每次问答的query、chunks、answer、metrics都会落库，评估人员可抽样检查bad case，分析错误来源（检索不足/生成幻觉/chunk边界问题）。此外计划引入用户反馈机制（点赞/点踩），通过用户行为数据持续优化系统。拒答准确率通过人工抽检约达80%，用于监控负例处理能力。

38. 如果LLM服务不可用或超时，你的系统如何保证可用性？
A: 系统实现了 多级降级策略 ：① LLM服务层设置超时限制（默认60秒），超时后返回友好提示"服务繁忙，请稍后重试"；② 支持流式输出降级为非流式：若流式接口失败，fallback到普通API调用；③ 检索结果兜底：当LLM完全不可用时，系统仍可返回检索到的chunks列表，让用户自行阅读；④ 配置切换：支持通过配置切换LLM服务商（阿里云/本地部署），主服务商失败时自动切换备选；⑤ 错误日志上报：所有失败请求记录详细错误信息，便于运维监控和问题定位。

39. 你如何保证RAG系统的安全性？有哪些防护措施？
A: 安全防护包括：① 输入过滤 ：对用户Query进行敏感词检测和长度限制，防止prompt注入攻击；② 输出审核 ：可配置LLM输出内容安全检测，过滤违规内容；③ 权限控制 ：API层验证用户身份和文档访问权限，用户只能查询有权限的会议记录；④ 日志审计 ：所有问答请求记录操作日志，支持事后审计；⑤ 速率限制 ：对API请求进行限流，防止恶意刷接口；⑥ 数据隔离 ：多租户场景下确保文档和检索结果严格隔离。

40. 你认为本项目中RAG板块的所有亮点，用于面试展示：

**一、架构设计亮点**
- **多层次检索融合**：向量检索（余弦相似度）+ BM25关键词检索 + BGE-Reranker交叉编码器精排，形成"召回-粗排-精排"三级漏斗，命中率提升约5%
- **灵活的Chunking策略**：支持Speaker模式（按发言者切分）和5种切分策略（fixed/paragraph/semantic/hybrid/recursive），针对会议场景优化，召回率从59.6%提升至86.1%
- **Query智能优化**：Query分解处理复杂多维度问题，HyDE假设文档提升召回率，Query扩展处理表达多样性，多策略按需组合

**二、技术选型亮点**
- **Embedding模型升级**：从MiniLM切换到BGE-M3，平均相似度从0.725提升至0.792，召回率从78.6%提升至85.6%，支持中英双语
- **轻量化部署**：使用PostgreSQL原生JSON/ARRAY存储向量，避免引入额外向量数据库依赖，降低运维复杂度
- **自适应Prompt**：根据Query类型（FACTUAL/SUMMARY/COMPARISON等）动态选择Prompt模板，推理型启用CoT，精细化优化

**三、工程实现亮点**
- **完整评估体系**：RAGAS六项指标 + 快速/深度双评估模式 + 233正例+30负例数据集，量化系统表现
- **异步处理架构**：全程异步设计，批量向量化优化大批量文档处理性能
- **流式输出**：基于SSE实现流式生成，实时返回LLM输出，用户体验好
- **知识图谱集成**：支持实体抽取+关系抽取，可构建结构化知识网络，支持多跳推理

**四、问题解决亮点**
- **Bad Case驱动优化**：碎片化发言合并、Overlap回溯优化、表格识别增强，典型问题逐一突破
- **参数调优有据可依**：Chunk size对比实验（256/512/1024）、Embedding模型A/B测试，用数据指导决策
- **容错机制完善**：LLM调用超时降级、语义分块失败自动回退规则分块、服务不可用时返回chunks列表

**五、数据指标亮点**
- 召回率：优化后稳定在 85% 左右
- 平均相似度：0.79
- MRR：0.75
- 命中率：43%-63%
- Embedding升级后相似度+7.5%，召回率+7%
- 拒答准确率：80%（人工抽检）


---

## Agent决策与工具调用板块问题清单（共 15 问）

### 1. Agent采用什么架构？为什么选择LangGraph？
A: 基于 LangGraph 实现 **Plan-Execute-Reflect** 工作流。核心优势：① 支持复杂任务拆解与依赖分析；② 状态持久化与 Checkpoint 机制；③ 内置循环与分支控制，比单纯 ReAct 更可控。项目中通过 `graph.py` 定义节点间数据流，实现任务并行执行与上下文传递。

### 2. Plan-Execute-Reflect三个阶段分别做什么？
A: **Plan 阶段**：分析用户问题，拆解为子任务列表，识别任务间依赖关系，规划并行/顺序执行策略。**Execute 阶段**：按计划调用工具执行子任务（retrieve/qa/minutes/todo/controversy 等），支持上下文传递和结果汇总。**Reflect 阶段**：评估执行结果质量，检测缺陷，生成改进建议，决定是否需要重新规划或补充执行。

### 3. Agent如何调用RAG作为工具？
A: 将 RAG 检索封装成 `search_meeting` 工具，输入用户问题，可选指定会议 ID 和返回数量，输出相关文档片段（包含 speaker、similarity、content 等字段）。工具定义在 `meeting_tools.py` 中，通过 ToolRegistry 统一管理，Agent 在 Execute 阶段根据任务需要选择性调用。前端 AgentDemo.vue 展示调用过程。

### 4. 除了RAG，还集成了哪些工具？
A: 从 `agents/tools/` 接口可查看：① extract_todos（待办抽取）：从会议内容中抽取待办事项，返回负责人和截止时间；② generate_minutes（会议纪要生成）：生成结构化会议纪要（主题/参会人员/讨论要点/决策事项/后续行动）；③ detect_controversies（争议点检测）：识别会议中的争议点和分歧；④ answer_question（问答回答）：根据上下文直接回答问题。所有工具支持 OpenAI 格式调用。

### 5. 工具是如何注册和管理的？
A: 通过 ToolRegistry 统一注册和管理工具，每个工具继承 BaseTool 基类，实现 get_definition() 定义工具元信息（名称、描述、参数、类别）和 execute() 执行逻辑。ToolExecutor 负责实际调用，ToolSelector 根据任务类型选择合适工具。工具分为 SEARCH、EXTRACT、GENERATE 等类别，支持动态扩展。

### 6. 会话记忆如何实现？
A: 短期记忆（进程内缓存，默认保留 100 条） + 长期记忆（支持关键词索引检索） + Checkpoint（LangGraph 状态保存）。当前主要为进程内，`memory.py` 中定义了完整的记忆系统：① MemorySystem 管理短期/长期/实体/关系；② ShortTermMemory/LongTermMemory 兼容类；③ MemoryCompressor 支持记忆压缩。未来计划持久化到数据库。

### 7. 短期记忆和长期记忆有什么区别？
A: **短期记忆**：存储当前会话的对话轮次（问题+答案），默认保留最近 100 条，支持 FIFO 淘汰，用于多轮对话上下文。**长期记忆**：存储跨会话的重要信息（关键事实、用户偏好、实体关系），支持关键词索引检索，可设置过期时间，用于个性化服务和知识积累。短期记忆可压缩整合后转为长期记忆。

### 8. 记忆压缩是怎么做的？
A: MemoryCompressor 在短期记忆超过阈值时触发：① 将多轮对话压缩为摘要；② 提取关键事实存入长期记忆；③ 清空已压缩的短期记忆。压缩策略：保留最近 N 轮原始对话 + 历史对话摘要。可调用 LLM 生成高质量摘要，或使用简单拼接（当前实现）。

### 9. 如何处理Agent死循环或错误？
A: 错误恢复系统（`agents/errors.py`） + 最大迭代次数限制（配置 MAX_TASKS_IN_PLAN = 10） + 降级回复。监控系统（`monitor.py`）记录失败链路，支持查询 Agent 执行日志和状态。此外还实现了计划验证（`plan_validator.py`）和自动修复机制，防止无效计划导致的循环。

### 10. 错误恢复机制具体怎么工作？
A: 多层级错误处理：① 工具层：工具执行失败返回 ToolResult(success=False, error=msg)，不中断整体流程；② 节点层：AgentNodes 捕获异常，记录日志，返回降级结果；③ 图层：LangGraph 支持条件边，根据状态决定重试或跳过；④ 服务层：AgentService 统一捕获，返回友好错误信息。错误分类：网络超时、LLM 调用失败、工具执行异常、计划解析错误等。

### 11. 流式输出怎么做的？
A: `POST /agents/query-stream` 使用 SSE 推送执行过程（计划→执行→反思→最终答案），后端通过 StreamingResponse 逐块 yield 状态更新和内容，前端使用 Fetch API 配合 EventSource 逐字显示。SSE 格式包含 event 类型（如 plan、execute、reflect、final）和 data 字段。

### 12. 人机确认接口有什么用？
A: 当 Agent 需要人工干预（如删除会议、发送邮件等敏感操作）时，等待用户确认后再执行，增加安全性。确认请求可以通过内存或数据库暂存，用户确认后继续执行任务。当前主要预留接口，未来可与实际业务场景集成。

### 13. 任务模板和计划验证是做什么的？
A: 任务模板（`task_templates.py`）：预设常见会议任务模板（如"生成会议纪要"、"抽取待办事项"），通过相似度匹配快速定位，无需每次都让 LLM 从零规划，提升效率。计划验证（`plan_validator.py`）：检查生成的任务计划是否符合语法、逻辑合理性、质量标准，支持自动修复简单问题，减少后续执行错误。

### 14. Agent如何处理复杂多任务？
A: 依赖分析 + 并行执行 + 上下文传递：① Plan 阶段识别任务间依赖关系（如任务 B 依赖任务 A 的结果）；② 将无依赖的任务分组并行执行；③ 有依赖的任务顺序执行，前序任务输出作为后续任务输入；④ Execute 阶段维护任务状态和结果缓存；⑤ 所有子任务完成后，Combine 任务汇总结果。示例："分析会议待办和争议点" → 并行执行 extract_todos + detect_controversies → 合并结果。

### 15. 本项目中Agent决策与工具调用板块的所有亮点，用于面试展示：
**一、架构设计亮点**
- **Plan-Execute-Reflect 工作流**：比单纯 ReAct 更可控，支持复杂任务拆解、依赖分析、并行执行
- **LangGraph 状态管理**：内置 Checkpoint 机制，支持断点续传和状态回溯
- **多层级错误恢复**：工具层→节点层→图层→服务层，层层兜底，系统健壮性强

**二、工具系统亮点**
- **5 个专用工具**：search_meeting、extract_todos、generate_minutes、detect_controversies、answer_question
- **统一工具管理**：ToolRegistry + ToolExecutor + ToolSelector，支持动态扩展
- **OpenAI 格式兼容**：工具定义可直接用于 OpenAI Function Calling

**三、记忆系统亮点**
- **三级记忆架构**：短期记忆（当前会话）+ 长期记忆（跨会话）+ Checkpoint（状态持久化）
- **记忆压缩机制**：自动压缩历史对话，提取关键事实，控制内存占用
- **实体关系管理**：支持实体抽取和关系构建，为知识图谱打基础

**四、工程实现亮点**
- **SSE 流式输出**：实时展示 Plan→Execute→Reflect 全过程，用户体验好
- **任务模板库**：预设常见任务模板，相似度匹配快速定位，减少 LLM 调用
- **计划验证与自动修复**：检查计划语法和逻辑，自动修复简单问题
- **人机确认接口**：敏感操作等待用户确认，安全性高


---

## 系统架构与工程实现板块问题清单（共 15 问）

### 1. 整体架构是什么样的？
A: 前后端分离：Vue 3 + Element Plus + FastAPI + PostgreSQL。前端通过 Vite proxy 转发 `/api` 到后端 8000 端口。后端采用分层架构：API 层（`api/v1/endpoints/`）→ 服务层（`services/`）→ 数据层（SQLAlchemy ORM + asyncpg）。核心模块：RAG 系统、Agent 系统、用户/会议/文档/待办 CRUD、配置中心、监控/日志/缓存。

### 2. 为什么选择FastAPI而不是Django或Flask？
A: **对比 Django**：Django 太重，自带 ORM、Admin、模板等，AI 应用不需要；FastAPI 轻量灵活。**对比 Flask**：Flask 无异步支持，需配合 gevent/eventlet；FastAPI 原生 async/await，性能更优。**FastAPI 优势**：自动生成 OpenAPI 文档（`/docs` 和 `/redoc`）、类型安全（Pydantic v2）、依赖注入系统、基于 Starlette 性能优秀。这些特性非常适合 AI 应用的快速迭代和稳定运行。

### 3. 为什么选择PostgreSQL而不是MySQL？
A: ① PostgreSQL 对 JSON/JSONB 支持更好，项目中用 JSON 存储向量和元数据；② PostgreSQL 支持数组类型，方便存储向量；③ PGVector 扩展可直接在 PostgreSQL 上做向量检索，未来可无缝升级；④ PostgreSQL 的全文搜索（tsvector）比 MySQL 强；⑤ 开源生态好，与 SQLAlchemy、asyncpg 兼容性佳。

### 4. 向量检索为什么不用专用向量数据库？
A: 初期数据量小（50 篇文档，约几百到几千个向量片段），PostgreSQL 存储向量完全够用（用 JSON/ARRAY 字段存储向量）。避免引入额外依赖（如 Milvus/Qdrant/Chroma 需要单独部署运维），降低项目复杂度。未来数据量增长时可迁移到 PGVector 扩展或专用向量数据库。

### 5. 如果数据量增长到百万级向量，如何扩展？
A: 迁移方案：① 启用 PGVector 扩展，创建 HNSW 或 IVF 索引加速检索；② 或迁移到专用向量数据库（Milvus/Qdrant），PostgreSQL 只存元数据；③ 水平分片：按会议 ID 或时间分表。架构优化：① 引入 Redis 缓存热点向量；② 异步批量检索；③ 读写分离，检索走只读副本。

### 6. 如何处理高并发下的检索性能？
A: 可加入 Redis 缓存（`core/cache.py` 已实现基础框架，CACHE_ENABLED 配置默认关闭）存储热点 Embedding 和检索结果；或用异步批量检索。当前架构支持水平扩展：API 无状态，可部署多实例；PostgreSQL 可读写分离。缓存装饰器 `cache_utils.py` 可轻松应用到任意异步函数。

### 7. 项目中哪些地方用到了缓存？
A: 缓存层在 `core/cache.py` 和 `utils/cache_utils.py`，当前未实际启用（CACHE_ENABLED = False），但已预留：① Redis 连接池管理；② @cache 装饰器；③ 缓存失效机制。可缓存用户信息、会议列表、Embedding 结果、检索结果等。Redis URL 配置在 Settings.REDIS_URL。

### 8. 如何保证 API 的稳定性？
A: 全局异常处理（`core/exceptions.py`，统一 AppException、HTTPException、ValidationException）、日志记录（loguru 按日滚动，`core/logger.py`）、重试机制（可在 Agent 调用 LLM 时配置）、超时控制（LLM 调用 20s 超时）、健康检查接口（`GET /health`）、中间件（`core/middleware.py` 访问日志）。

### 9. 异常处理机制是怎么设计的？
A: 分层异常处理：① 自定义 AppException（业务异常，包含 code、message）；② 全局异常处理器（`app_exception_handler`、`http_exception_handler`、`validation_exception_handler`、`global_exception_handler`）；③ 统一响应格式（Response(code, message, data)）；④ 日志记录异常堆栈。业务代码抛 AppException，框架自动捕获并返回友好错误信息。

### 10. 数据库表设计有哪些？
A: 6 张核心表：① users（用户表：用户名、邮箱、密码哈希、部门、角色）；② meetings（会议主表：标题、状态、原文、摘要、纪要、关键词）；③ speech_records（发言记录表：关联会议、发言人、时间偏移）；④ documents（文档库表：文件路径、解析内容、关联会议）；⑤ todo_items（待办表：标题、负责人、优先级、状态、截止时间）；⑥ vector_chunks（向量知识库表：文本片段、向量、元数据）。

### 11. 表之间的关联关系是怎样的？
A: ① meetings ← speech_records（一对多，一个会议多条发言）；② meetings ← documents（一对多，一个会议多个关联文档）；③ meetings ← todo_items（一对多，一个会议多个待办）；④ documents ← vector_chunks（一对多，一个文档多个切片）；⑤ users ← meetings（多对多，参会人员）；⑥ users ← todo_items（一对多，用户负责的待办）。

### 12. 配置如何管理？
A: 采用 pydantic-settings（`core/config.py`）：① 从 .env 文件读取环境变量；② 提供默认值；③ 类型安全；④ 支持热加载（`config_center.py`）。配置项包括：数据库、Redis、LLM、Embedding、RAG 参数、Agent 参数等。可通过 API `GET/POST /config` 动态调整。

### 13. 安全措施有哪些？
A: JWT 认证（`core/security.py`，python-jose + bcrypt）、密码哈希、权限控制（依赖注入 `get_current_user`）、CORS 配置（`CORSMiddleware`，仅允许前端地址）、输入验证（Pydantic）、日志审计（所有操作记录日志）、速率限制（预留，可通过中间件实现）。用户只能访问自己有权限的会议和文档。

### 14. 日志系统是怎么设计的？
A: 使用 loguru（`core/logger.py`）：① 按日滚动，自动清理过期日志；② 分级输出（DEBUG/INFO/WARNING/ERROR/CRITICAL）；③ 同时输出到控制台和文件；④ 格式化输出（时间、级别、模块、消息）；⑤ 异常堆栈自动捕获。日志目录配置在 LOG_DIR，级别配置在 LOG_LEVEL。关键操作（如 LLM 调用、检索、Agent 执行）都有详细日志。

### 15. 本项目中系统架构与工程实现板块的所有亮点，用于面试展示：
**一、架构设计亮点**
- **前后端分离**：Vue 3 + FastAPI，职责清晰，独立开发部署
- **分层架构**：API 层 → 服务层 → 数据层，代码组织清晰，易维护
- **异步全栈**：FastAPI async/await + asyncpg + AsyncOpenAI，I/O 密集场景性能优

**二、技术选型亮点**
- **PostgreSQL 多用途**：关系数据 + JSON 存向量 + 全文搜索，一库多用
- **Pydantic v2 类型安全**：请求验证、响应序列化、配置管理，类型错误早发现
- **loguru 日志**：按日滚动、分级输出、异常捕获，运维友好

**三、工程化亮点**
- **全局异常处理**：统一错误响应格式，前端易处理
- **配置中心**：pydantic-settings + .env + 热加载，环境隔离
- **健康检查接口**：`GET /health`，支持容器编排监控
- **访问日志中间件**：记录所有 API 请求，便于审计和问题定位

**四、扩展性亮点**
- **无状态 API**：支持水平扩展，多实例负载均衡
- **缓存预留**：Redis 缓存框架已实现，一键开启
- **向量库可迁移**：当前 PostgreSQL，未来可无缝迁移到 PGVector 或 Milvus


---

## 大模型调用与优化板块问题清单（共 15 问）

### 1. 用了哪个大模型？为什么？
A: 使用阿里云通义千问 `qwen3.6-plus` 作为主模型，`qwen-turbo` 作为评估专用轻量模型。选型理由：① 成本可控（相比 GPT-4 便宜）；② 中文能力强（会议场景主要是中文）；③ 速度快；④ 兼容 OpenAI 接口（`llm_service.py` 用 AsyncOpenAI 客户端调用，只需配置 BASE_URL 和 API_KEY）。

### 2. 为什么不用GPT-4或开源模型？
A: **对比 GPT-4**：成本高，会议场景大量调用不划算；国内访问不稳定。**对比开源模型（如 Llama、Qwen 本地部署）**：需要 GPU 资源，部署运维成本高；小模型效果不如商业模型。**选择 qwen3.6-plus**：性价比高，中文效果好，API 稳定，适合中小厂快速落地。

### 3. LLM服务是如何封装的？
A: `llm_service.py` 封装 AsyncOpenAI 客户端：① 统一配置（API_KEY、BASE_URL、TIMEOUT、MAX_TOKENS）；② chat() 方法通用对话；③ generate_answer() 方法 RAG 专用（构建 system_prompt + user_message）；④ 支持流式输出（stream=True）；⑤ 错误处理和日志记录。所有 LLM 调用都通过 LLMService，便于统一管理和切换模型。

### 4. Prompt模板怎么设计的？
A: 参考 `agents/prompts.py`，针对规划、执行、反思等不同角色设计专门模板，支持动态变量。RAG 生成 prompt 采用双层结构：系统提示设定角色和约束（不编造、简洁），用户消息包含原始问题、参考信息片段。自适应 Prompt（`adaptive_prompt.py`）根据 Query 类型（事实/总结/对比/推理/列表）选择对应模板。

### 5. 自适应Prompt是如何工作的？
A: `adaptive_prompt.py` 实现：① QueryClassifier 分析问题类型（基于关键词 + LLM 判断）；② 根据类型路由到对应模板：FACTUAL（简洁准确）、SUMMARY（结构化组织）、COMPARISON（CoT 推理，突出差异）、REASONING（列推理步骤）、LIST（逐条列出）；③ 每种类型有独立的 system_prompt 和 user_template；④ 动态注入上下文和问题。

### 6. 如何控制输出格式和减少幻觉？
A: 要求 JSON 模式输出（如待办抽取、争议点检测），结合 RAG 检索内容约束生成（明确提示"如果参考信息中没有，不要编造"），并加入答案相似度评估。检索时会将 similarity 分数传给 LLM 作为参考。对低置信度结果可触发降级回复或追问用户。

### 7. 如何让LLM输出JSON格式？
A: 两种方式：① Prompt 中明确要求 JSON 格式，给出示例 schema，用正则从响应中提取 JSON；② 使用 OpenAI 的 response_format={"type": "json_object"} 参数（部分模型支持）。项目中待办抽取、争议点检测等工具都用 JSON 输出，便于后续解析和处理。

### 8. 支持流式输出吗？如何实现？
A: 支持 SSE 流式输出。RAG 流式：`POST /rag/ask-stream`，Agent 流式：`POST /agents/query-stream`。后端使用 FastAPI StreamingResponse，通过 yield 逐块输出；前端用 Fetch API + ReadableStream 逐字渲染，提升用户体验。流式输出减少首字等待时间。

### 9. 流式输出和非流式输出如何切换？
A: API 层面：`/rag/ask` 非流式返回完整 JSON，`/rag/ask-stream` 流式返回 SSE。服务层面：LLMService.chat() 支持 stream 参数，stream=True 返回异步生成器，stream=False 返回完整字符串。降级策略：流式失败时自动 fallback 到非流式。

### 10. 成本怎么控制？
A: ① 设置 max_tokens 限制（配置 LLM_MAX_TOKENS = 1000）；② 对小任务用更便宜模型（如评估用 qwen-turbo，EVAL_LLM_MODEL 配置）；③ 缓存常见问题的 RAG 结果（预留缓存层）；④ Context 压缩（只送 top 相关片段，LLM_MAX_CONTEXT_CHARS = 5000 字符限制）；⑤ Prompt 优化（避免冗余，提升效率）。

### 11. LLM调用失败如何处理？
A: 多级降级策略：① LLM 服务层设置超时（20s），超时返回友好提示；② 支持流式输出降级为非流式；③ 检索结果兜底（当 LLM 完全不可用时，返回检索到的 chunks 列表让用户自行阅读）；④ 配置切换（支持通过配置切换 LLM 服务商）；⑤ 错误日志上报（所有失败记录详细日志）。

### 12. 如何处理上下文长度限制？
A: Context 管理策略：① 逐个添加 chunk 直到达到 LLM_MAX_CONTEXT_CHARS 限制；② 后一个 chunk 可截断但至少保留 100 字符；③ 超出的 chunk 直接丢弃；④ 预留上下文压缩接口（可基于相似度或摘要进一步压缩）。对超长文档依赖切片策略（speaker 或 fixed）。

### 13. 上下文压缩有哪些策略？
A: 三种策略：① **按比例压缩**：只保留 top N 个最相关 chunk；② **按相关性压缩**：丢弃 similarity 低于阈值的 chunk；③ **基于摘要压缩**：用 LLM 对多个 chunk 生成摘要，摘要替代原文。当前实现第一种，后两种预留接口。压缩后保留 chunk 编号，便于答案溯源。

### 14. 有没有做 Prompt 优化或 A/B 测试？
A: 当前通过自适应 Prompt 根据 Query 类型选择模板（对比型用 CoT，推理型列推理步骤）。A/B 测试框架：预留接口，可对比不同 Prompt、模型、参数的效果（通过 RAGAS 指标或人工评估）。未来计划增加 Prompt 模板市场（`prompt_market.py`），支持在线测试和切换。

### 15. 本项目中大模型调用与优化板块的所有亮点，用于面试展示：
**一、模型选型亮点**
- **性价比优先**：qwen3.6-plus 主模型 + qwen-turbo 评估模型，成本可控
- **OpenAI 兼容接口**：一行配置切换模型，不绑定单一厂商
- **异步调用**：AsyncOpenAI 客户端，并发调用性能优

**二、Prompt 工程亮点**
- **自适应 Prompt**：根据 Query 类型（事实/总结/对比/推理）自动选择模板
- **双层结构**：system_prompt 设定角色约束 + user_message 注入上下文
- **JSON 输出控制**：明确要求格式 + 正则提取，工具调用可靠

**三、成本控制亮点**
- **多级限制**：max_tokens + context_chars + 便宜模型用于轻量任务
- **上下文压缩**：只送相关 chunk，减少无效 token 消耗
- **缓存预留**：常见问题缓存，避免重复调用

**四、稳定性亮点**
- **多级降级**：超时 → 非流式 → 检索结果兜底，层层保障
- **流式输出**：SSE 实时返回，首字延迟低，用户体验好
- **错误日志**：所有 LLM 调用记录详细日志，便于问题定位


---

## 评估与迭代板块问题清单（共 12 问）

### 1. 你们怎么量化RAG效果？
A: 使用评估数据集（233 正例 + 30 负例），计算 Recall@K、Precision@K、MRR、Answer Similarity（快速用 BGE-M3 向量相似度，深度用 LLM 判断）。通过 EvaluationPage.vue 运行评估，支持单次评估和批量评估（`rag_evaluation_service.py`、`ragas_evaluator.py`）。RAGAS 集成 Faithfulness、Answer Relevance、Context Precision、Context Recall、Answer Correctness 等指标。

### 2. RAGAS各项指标的含义是什么？
A: ① **Faithfulness**：答案是否忠实于检索上下文，有无幻觉；② **Answer Relevance**：答案与问题的相关程度；③ **Context Precision**：检索上下文的精确度，无关内容占比；④ **Context Recall**：检索上下文是否覆盖答案所需信息；⑤ **Answer Similarity**：答案与标准答案的语义相似度；⑥ **Answer Correctness**：答案正确性综合评分。每项 0-1 分，越高越好。

### 3. 干扰文档对结果有多大影响？
A: 数据集包含 22% 干扰文档（旅游攻略等无关内容），测试表明：① 纯向量检索 Recall 下降约 5-10%；② BM25 + 向量融合召回能缓解（下降约 2-5%）；③ 重排序（Rerank）能进一步提升 Precision。可以展示优化前后的对比数据（在 `tests/` 目录有评估结果记录）。

### 4. 有没有做A/B测试？
A: 没做线上 A/B，但在评估模式下对比不同切分模式（speaker vs fixed）、不同 top_k、不同 Embedding 模型、是否开启 Rerank 的效果。评估结果包含各指标对比，支持导出 CSV 或图表。未来计划增加 A/B 测试框架，自动统计显著性。

### 5. 如何根据评估结果迭代？
A: 分析思路：① 若 Recall 低 → 优化检索（换 Embedding、增加 BM25 权重、调整 chunk size）；② 若 Precision 低 → 优化重排序或 Context 压缩；③ 若 Answer Similarity 低 → 优化 Prompt 或增加上下文长度；④ 若负例拒答差 → 增强阈值判断或专门的拒答分类器。用数据驱动决策，而非盲目调整。

### 6. 评估数据集如何构建？
A: 半自动构建：人工设定要求（覆盖不同会议类型、问题难度、正负例）→ LLM 批量生成 → 人工筛选和调整 → 存入评估数据集。正例 233 条（有明确答案），负例 30 条（无答案）。问题类型包括事实型、推理型、总结型、对比型等，保证覆盖度。

### 7. 正例和负例分别如何评估？
A: **正例评估**：计算 Recall@K（标准答案是否在检索结果中）、Precision@K（检索结果中有多少相关）、MRR（标准答案排名）、Answer Similarity（生成答案与标准答案相似度）。**负例评估**：期望系统拒答或返回"未找到"，计算拒答准确率（正确拒答 / 负例总数）。

### 8. 有回归测试吗？
A: 预留回归测试框架（`rag_regression.py`），每次代码改动后可自动运行评估，对比历史基线，防止性能回退。测试数据集固定，保证评估一致性。单元测试在 `tests/unit/`，集成测试在 `tests/integration/`。

### 9. 快速评估和深度评估有什么区别？
A: **快速评估**（EVAL_SKIP_LLM=True）：只计算检索指标（Recall、Precision、MRR），不调用 LLM 生成答案，速度快，适合日常调参。**深度评估**（EVAL_SKIP_LLM=False）：计算全部 RAGAS 指标，包括 LLM 评估的 Faithfulness、Answer Relevance 等，更全面但耗时。评估模型用 qwen-turbo，成本可控。

### 10. 用户反馈如何收集和利用？
A: 当前系统未收集用户反馈，但预留接口：① 可记录用户对答案的点赞/点踩；② 可关联反馈与检索 chunks；③ 可定期用反馈数据 fine-tune 重排序模型或调整 Prompt。未来计划增加用户反馈管理页面和反馈分析仪表盘。

### 11. 如何分析Bad Case？
A: 分析流程：① 从评估结果筛选低分样本（Recall < 0.5 或 Answer Similarity < 0.6）；② 检查检索结果：是否召回正确 chunk？相似度分数分布？③ 检查生成结果：是否幻觉？是否遗漏关键信息？④ 定位原因：chunk 切分问题？Embedding 不匹配？Prompt 不合适？⑤ 针对性优化并验证。日志和评估结果都记录详细信息，便于回溯。

### 12. 本项目中评估与迭代板块的所有亮点，用于面试展示：
**一、评估体系亮点**
- **RAGAS 集成**：6 项核心指标，全面评估检索和生成质量
- **快速/深度双模式**：快速评估调参，深度评估验证，效率与质量兼顾
- **正负例分离评估**：正例算召回相似度，负例算拒答准确率，覆盖全面

**二、数据集亮点**
- **半自动构建**：人工设定要求 + LLM 批量生成 + 人工筛选，效率与质量兼顾
- **干扰文档测试**：22% 干扰文档，测试系统鲁棒性
- **多类型覆盖**：事实型、推理型、总结型、对比型，覆盖真实场景

**三、迭代流程亮点**
- **数据驱动决策**：根据指标定位问题，针对性优化，不盲目调参
- **Bad Case 分析**：详细日志 + 评估结果，便于回溯和定位
- **回归测试预留**：代码改动后自动对比基线，防止回退

**四、可视化亮点**
- **评估页面**：EvaluationPage.vue 可视化运行评估，结果直观
- **对比图表**：不同配置效果对比，一图胜千言
- **结果导出**：支持 CSV 导出，便于进一步分析


---

## 项目经验与个人总结板块问题清单（共 12 问）

### 1. 项目中最大的技术难点是什么？怎么解决的？
A: 难点 1：按说话人切片时，单个发言太短导致检索效果差 → 解决方案：实现短发言合并策略（SPEAKER_MIN_CHUNK_SIZE = 100），Recall 从 59.6% 恢复到 86.1%。难点 2：纯向量检索对精确匹配（如人名、项目名）不如关键词 → 解决方案：引入 BM25 + 向量融合，命中率提升约 5%。难点 3：LLM 生成的 chunk 边界不合理 → 解决方案：增加自动回退到规则切分（paragraph 或 fixed）的机制。

### 2. 在项目中你遇到过哪些典型的 Bad Case，怎么解决的？
A: Bad Case 1：Speaker 切分后单个 chunk 只有 "好的" 两字，完全无法检索 → 解决：增加短发言合并，连续同一发言人且 chunk < 100 字则合并。Bad Case 2：PDF 表格被切分在不同 chunk，跨 chunk 信息丢失 → 解决：预留表格识别和单独向量化接口（尚未完全实现）。Bad Case 3：用户问"张三和李四吵什么"，纯向量检索找不到 → 解决：计划引入实体关系抽取和知识图谱。

### 3. 如果重新做，会改进哪里？
A: ① 引入更强大的 Embedding（如 bge-large 或更新的多语言模型）；② 支持在线学习（用户反馈纠正向量或重排序）；③ Agent 记忆持久化（Redis/数据库，而非仅进程内）；④ 更完善的表格处理（单独向量化表格）；⑤ 前端页面完整开发（当前部分为演示用）；⑥ 增加真实业务场景的集成测试。

### 4. 你的项目跟其他RAG开源项目有什么不同？
A: 聚焦会议场景（而非通用文档问答），特色：① 按说话人切片，保留发言者信息；② 集成 Agent 任务规划（Plan-Execute-Reflect），不仅是问答，还支持会议纪要生成、待办抽取、争议点检测等复杂任务；③ 完整的评估体系（RAGAS + 自建数据集 + 评估页面）；④ 工程化程度高（FastAPI 后端、Vue 前端、分层架构、配置管理、日志监控）。

### 5. 你觉得自己做得最好的一部分是什么？
A: ① 评估系统的完整性（可量化、可回归测试、可视化页面），对面试官很直观；② 多路召回 + 重排序的实现，效果提升明显；③ Agent 系统的架构设计（LangGraph + 工具调用 + 记忆 + 监控），扩展性好；④ 代码质量（分层清晰、注释完善、有测试用例）。

### 6. 后续计划怎么扩展？
A: 短期：① 完善前端功能；② 增加更多会议工具（如情绪分析、发言时长统计）；③ 优化表格处理；中期：① 支持多轮对话和上下文理解；② 接入企业微信/钉钉机器人；③ 多人协作确认待办；长期：① 引入知识图谱，支持多跳推理；② 支持实时会议转录和实时问答；③ 私有化部署方案。

### 7. 如何保证代码质量和可维护性？
A: ① 分层架构（API/服务/数据层分离）；② 类型注解（Python typing + Pydantic）；③ 日志完善（loguru 分级记录）；④ 单元测试和集成测试（pytest）；⑤ 代码注释和文档（README、接口文档）；⑥ Git 版本管理（虽然当前单用户，但遵循规范）；⑦ 配置与代码分离（Settings + .env）。

### 8. 项目中用到了哪些设计模式或最佳实践？
A: ① **依赖注入**：FastAPI 的 Depends() 注入数据库会话、当前用户；② **工厂模式**：LLMService、VectorSearchService 等服务类实例化；③ **策略模式**：不同切分策略（speaker/fixed/semantic）、不同检索策略；④ **装饰器模式**：@cache 缓存装饰器；⑤ **单一职责**：每个服务类只负责一个领域；⑥ **开闭原则**：工具系统支持扩展，无需修改核心代码。

### 9. 面试时如何展示这个项目？
A: 展示策略：① 先讲整体架构图（前后端 + RAG + Agent），让面试官有全局观；② 演示核心功能（RAG 问答、Agent 任务规划），用实际效果说话；③ 展示评估数据和优化过程（Recall 从 60% 到 86%），体现数据驱动；④ 讲 1-2 个 Bad Case 的解决故事，体现分析和解决问题能力；⑤ 强调工程化（异步、缓存、日志、监控），而非仅算法 demo。

### 10. 这个项目有哪些不足或局限？
A: ① 前端部分页面为演示用，不够完善；② Agent 记忆仅进程内，未持久化；③ 表格处理不够完善；④ 未做线上 A/B 测试；⑤ 未收集用户反馈；⑥ 知识图谱模块未与主检索链路打通。坦诚承认不足，并说明改进方向，反而体现思考深度。

### 11. 从这个项目中学到了什么？
A: 技术层面：① RAG 不是简单的向量检索，需要多路召回、重排序、Query 优化等全链路优化；② Agent 需要完善的错误处理和监控，否则很容易出问题；③ 评估体系非常重要，没有量化指标就无法科学优化。工程层面：① 分层架构和类型安全让代码更易维护；② 异步和缓存对性能提升明显；③ 日志和监控是生产环境必备。

### 12. 本项目中项目经验与个人总结板块的所有亮点，用于面试展示：
**一、问题解决能力亮点**
- **3 个典型 Bad Case**：碎片发言、表格切分、实体检索，逐一分析解决
- **数据驱动优化**：Recall 从 59.6% 到 86.1%，用数据说话
- **多方案对比**：不同 Embedding、不同切分策略、不同权重，科学选型

**二、工程能力亮点**
- **完整工程化**：前后端分离、分层架构、配置管理、日志监控
- **代码质量高**：类型注解、单元测试、注释完善、设计模式应用
- **可扩展设计**：工具系统、切分策略、检索策略都支持扩展

**三、思考深度亮点**
- **坦诚不足**：前端不完善、记忆未持久化、表格处理待优化，并给出改进方向
- **对比分析**：与开源 RAG 项目对比，明确差异化优势
- **总结经验**：技术层面 + 工程层面双总结，体现成长

**四、展示策略亮点**
- **一图胜千言**：架构图 + 评估对比图 + 演示效果
- **故事化表达**：Bad Case 分析故事，比纯技术描述更生动
- **突出工程化**：不只是算法 demo，而是可落地的工程系统


---

## 综合面试准备要点

### 高频问题方向
1. **RAG 全流程**：从文档上传 → 解析 → 切片 → 向量化 → 存储 → 检索 → 重排序 → 生成的完整链路
2. **技术选型理由**：为什么用 PostgreSQL 不用向量库？为什么用 bge-m3 不用其他？为什么用 LangGraph？
3. **性能指标**：Recall、Precision、MRR、Answer Similarity 的具体数值和优化过程
4. **Bad Case 分析**：准备 2-3 个具体例子，如何发现、分析、解决
5. **系统设计**：如何扩展到 10 万用户？如何处理高并发？如何保证数据安全？

### 展示材料建议
1. 架构图（前后端 + RAG + Agent）
2. 评估对比图表（不同模型/切分策略的效果）
3. 1-2 个 Bad Case 的前后对比
4. 核心代码片段（如多路召回融合、LangGraph 工作流）
5. 演示环境（能运行前端页面，展示问答和 Agent 效果）