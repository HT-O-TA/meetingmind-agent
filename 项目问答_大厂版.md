
---

## MeetingMind 面试问答 - 大厂版

> 注：此版本增加技术深度、架构思考、前沿探索内容，适合准备大厂面试。

---

## RAG 板块问题清单（共 35 问）

### 一、核心架构与选型（10问）

1. 详细讲一下你的 RAG 全流程？
A: 文档上传 → 格式解析（txt/md/pdf/docx/csv/xlsx，表格统一转 Markdown）→ 切片（支持 speaker/fixed/paragraph/semantic/hybrid，speaker 适合会议场景，短发言合并）→ 向量化（bge-m3，1024维）→ 存储（PostgreSQL JSON/ARRAY字段）→ 检索（向量检索+BM25多路召回，权重0.7:0.3）→ 重排序（BGE-Reranker）→ 生成（qwen3.6-plus，Prompt包含上下文和问题）→ 输出（支持流式）。

2. 你为什么选择 bge-m3 而不是其他 Embedding 模型？深入分析一下？
A: 对比过：MiniLM-L6-v2（效果略差）、bge-base（效果好一些）、bge-large（更好但速度慢）、OpenAI text-embedding-3-small（贵，国内访问问题）。最终选 bge-m3：① 支持中英双语，会议场景合适；② Multilingual 效果好，MTEB 榜单排名高；③ 轻量但效果好，平均相似度从 0.725 到 0.79；④ 支持多向量（dense + sparse + colbert），虽然当前只用了 dense，但预留空间；⑤ 开源，可本地部署，成本可控。

3. 多路召回为什么选 BM25+向量，权重为什么是0.3:0.7？
A: 向量检索擅长语义相似，BM25擅长关键词精确匹配（如人名、项目名、会议号），两者互补。权重是调出来的：向量 1.0 → Recall 82%，Precision 70%；BM25 1.0 → Recall 75%，Precision 75%；0.3:0.7 → Recall 85.6%，Precision 73%，综合最优。未来可以用 Learning to Rank 或基于验证集自动调参。

4. 重排序为什么用 BGE-Reranker-v2-m3？有做对比吗？
A: 对比过：CrossEncoder（不错）、bge-reranker-v2-m3（更好，中英双语）、ColBERT（慢）、RRF（不需要额外模型但效果差一些）。选 bge-reranker-v2-m3：① 专门为双语优化；② 模型不大，推理快；③ 效果提升3%左右；④ 与 embedding 同一系列，兼容性好。

5. 向量相似度为什么用余弦而不是点积或欧氏距离？
A: ① 查询向量和文档向量长度不固定，余弦归一化了长度，只看方向；② 会议场景关注主题相关，不管长短；③ bge 等模型训练时用的是余弦相似度；④ 点积受长度影响，欧氏距离对长度敏感。如果向量归一化了，余弦和点积等价。

6. 为什么当前不用向量索引（HNSW/IVF）？有做过对比吗？
A: 当前数据量小（50篇文档，约几千个chunk），全量扫描足够快。对比过：① HNSW 图索引：精度高，内存占大，构建慢，小数据集没必要；② IVF 聚类索引：构建快，查询略快，精度稍低；③ 全量扫描：简单，精确，当前够用。未来数据到 10万+ 再上 PGVector + HNSW，已预留架构。

7. 如果数据量增长到百万级甚至十亿级，你的架构怎么演进？
A: 演进路径：① 阶段1（10万级）：启用 PGVector 扩展，HNSW 索引，Redis 缓存热点；② 阶段2（百万级）：迁移到专用向量库（Milvus/Qdrant），只存元数据和向量，PostgreSQL 存原始数据；③ 阶段3（千万-亿级）：向量分片（按会议ID/时间/租户），读写分离，检索走副本，分层缓存（L1本地缓存、L2 Redis、L3向量库）；④ 阶段4（十亿级）：分布式向量库（Milvus 集群），冷热数据分离，离线批量索引，在线增量更新，可能用 Approximate Nearest Neighbor 牺牲小部分精度换效率。

8. 你考虑过 GraphRAG 吗？为什么当前没做？
A: 考虑过，当前没做的原因：① 项目初期，先把基础 RAG 做好；② 会议场景下，GraphRAG 的多跳推理收益不明显；③ 增加复杂度，构建成本高；④ 但已预留实体抽取和关系抽取模块，知识图谱构建了一半，未来可以结合。GraphRAG 适合复杂推理问题，当前先够用，再考虑增强。

9. Query 改写为什么选 Query分解 + HyDE + 扩展的组合？
A: Query分解解决复杂问题（如"张三和李四分别负责什么"），HyDE（Hypothetical Document Embedding）生成假设文档作为查询，提升召回，解决"以文找文"更准的问题，Query扩展解决同义词和表达多样性问题。组合起来覆盖不同情况，可配置开关，灵活。

10. 你对比过哪些 RAG 框架？为什么自己写？
A: 对比过 LangChain（太重，抽象多）、LlamaIndex（偏向文档）、Haystack（复杂）。自己写的原因：① 更可控，定制化强，针对会议场景优化；② 轻量，没有不必要的依赖；③ 学习底层原理，面试能讲清楚；④ 中小厂场景不需要那么复杂的框架。当然，如果是大厂生产环境，可能会基于现有框架二次开发。

### 二、切片与优化（8问）

11. 详细讲一下 Speaker 模式的切分逻辑？怎么判断发言边界？
A: 五种正则模式匹配：①"张三："发言内容；②"[10:30] 张三 说："；③"张三(产品经理)："；④ MARKDOWN 的标题或列表项；⑤ 退而求其次按段落。匹配到发言人后，提取 speaker_name, time_offset，然后收集该发言人的内容，直到下一个发言人出现。如果太乱找不到，降级 fixed 模式。

12. 短发言合并策略具体是怎么设计的？有参数可调吗？
A: SPEAKER_MIN_CHUNK_SIZE = 100（可调），如果当前发言 + 上一同发言人发言 < 阈值，合并。合并时保留元数据（记录是合并的）。解决了"好的""对的""可以"这种短发言无法检索的问题，Recall 从 59.6% 提升到 86.1%。

13. 语义切分是怎么做的？LLM 切分失败怎么兜底？
A: 语义切分可选，当前默认关闭。逻辑：① LLM 分析语义边界（用 qwen-turbo，便宜）；② 输出 JSON 格式的切分点；③ 如果输出格式不对、超时、失败，自动降级到 paragraph 规则切分。语义切分质量高但慢且费钱，适合小批量精标数据。

14. 固定长度切分的 512 和 overlap 64 是怎么确定的？有实验吗？
A: 经验值但有对比：测试过 256（太碎）、512（平衡）、1024（上下文完整但召回下降）。512 是中文长文档常用值。overlap 64 防止边界信息丢失，太大（128）重复太多，太小（32）可能不够。未来可以用验证集调参。

15. 表格是怎么处理的？切分的时候怎么保证表格完整性？
A: 表格统一转 Markdown 格式存储，切分的时候如果遇到表格边界，保留整个表格作为一个 chunk 或完整纳入下一个 chunk，避免表格被切断丢失信息。当前是规则处理，未来可以用 LLM 识别表格结构，单独向量化表头和内容。

16. Context 窗口超限怎么处理？详细讲一下策略？
A: LLM_MAX_CONTEXT_CHARS = 5000（可调），策略：① 按 similarity 排序，先加最相关的；② 逐个加 chunk，直到接近限制；③ 最后一个 chunk 可以截断，但至少保留 100 字符；④ 超出的丢弃；⑤ 预留接口支持压缩（当前没开）：可以用 LLM 把多个 chunks 摘要成一个，或者按比例压缩。

17. 你考虑过 Parent-Child Chunking（父块子块）吗？
A: 考虑过，预留了 SEMANTIC_CHUNK_BUILD_HIERARCHY 配置。思路：小 chunk（256）用于检索，保证精度；父 chunk（1024）用于生成，保证上下文完整。检索返回子块，但把对应的父块送进 LLM。当前没开是因为增加复杂度，先 baseline 做好，未来可以加。

18. 有做 Chunk 元数据过滤吗？怎么利用？
A: 有，chunk 元数据包含：会议ID、文档ID、发言人、时间偏移、部门。检索时可以先按元数据过滤（如"只查张三的发言"或"只查2023年的会议"），再向量检索，提升精度和速度。

### 三、幻觉与质量控制（8问）

19. 你怎么减少幻觉？有哪些措施？
A: 多层防护：① 检索层：保证召回的 chunk 相关，重排序提升质量；② Prompt层：明确要求"如果参考信息中没有，请说不知道"，"不要编造"，把 similarity 分数告诉 LLM 作为参考；③ 生成层：设置 max_tokens 限制，温度 0.7 不要太高；④ 验证层：当前没做，未来可以加 Answer Similarity 或用 LLM 自检；⑤ 拒答层：如果所有 chunk similarity &lt; 0.7，提示"没找到相关信息"。

20. 你怎么评估答案质量？有自动化方法吗？
A: RAGAS 6大指标：① Faithfulness（忠实度，用 LLM 检查答案是否来自上下文）；② Answer Relevance（答案与问题的相关性）；③ Context Precision（上下文精度）；④ Context Recall（上下文召回）；⑤ Answer Similarity（与标准答案的相似度，bge-m3 算）；⑥ Answer Correctness（综合评分）。快速模式跳过 LLM 只算检索指标，深度模式全算。

21. 负例问题（没有答案的）怎么处理？
A: 数据集中有 30 个负例，评估拒答准确率。期望系统返回"未找到相关信息"。当前做法是看 similarity 阈值，未来可以训练专门的拒答分类器，或者用 LLM 判断"有没有答案"。

22. 有没有做过基于用户反馈的优化？
A: 当前没做，但预留了接口：记录点赞/点踩、关联 query 和 chunks、记录用户修正。未来可以：① 用反馈数据 fine-tune 重排序模型；② 用 Hard Negative 增强检索；③ 强化学习 RLHF；④ 把用户确认的正确答案加入知识库。

23. 如果检索到的是错误或过时的信息，怎么处理？
A: 多层防护：① 数据源层面：设置文档过期时间，旧文档权重降低；② 检索层面：按时间衰减，新文档优先；③ 元数据过滤：可指定时间范围；④ Prompt 层面：告诉 LLM"如果信息过时，请指出"；⑤ 未来：版本控制，知识更新机制，冲突检测。

24. 怎么处理多文档、跨会议的信息整合？
A: 当前是召回后全送进去，LLM 自己整合。未来可以：① Query 分解，先分别查，再汇总；② Map-Reduce：每个文档生成摘要，再综合；③ Refine：逐文档 refine 答案；④ Agent 多步规划整合。

25. 有没有做过 A/B 测试？怎么设计的？
A: 当前没线上 A/B，但有离线评估。如果线上：① 分流（50% old，50% new）；② 指标：用户点赞率、Answer Similarity、Session 时长、任务完成率；③ 统计显著性检验（t-test）；④ 置信度 95%。评估框架预留了。

26. 你对 RAG 的未来发展怎么看？有什么前沿方向？
A: 前沿方向：① GraphRAG + 知识图谱增强多跳推理；② Multi-Modal RAG（文本+图片+音频+视频）；③ RAG + Agent 深度结合；④ 检索增强微调（RAFT），不是纯 RAG 也不是纯微调，结合两者；⑤ 检索结果的自动压缩和摘要；⑥ 个性化 RAG，根据用户历史和偏好调整；⑦ RAG 的可解释性，显示答案来源，高亮参考片段。

### 四、工程化与亮点总结（9问）

27. 你的系统是异步的？详细讲一下异步流程？
A: 全链路异步：① FastAPI 异步端点；② SQLAlchemy 2.0 asyncpg 异步数据库；③ AsyncOpenAI 异步调用 LLM；④ 异步向量化（可以批量并行）。好处：高并发下不阻塞，吞吐高，适合 I/O 密集的 AI 应用。

28. 缓存怎么设计的？当前没启用，未来怎么加？
A: 预留了缓存层：① 多级缓存：L1本地（LRU）、L2 Redis；② Key 设计：Query 哈希 + embedding 版本 + top_k 参数；③ TTL：可配置，默认 5 分钟；④ 装饰器模式 @cache，对 service 层方法加；⑤ 缓存失效：文档更新时清除相关缓存。当前数据量小，没启用，未来加上预计 QPS 提升 3-5 倍。

29. 日志系统怎么设计的？
A: loguru 配置：① 按日滚动，保留 30 天；② 分级：DEBUG（开发）、INFO（生产主流程）、WARNING（警告）、ERROR（错误）、CRITICAL（严重）；③ 格式化：时间、级别、模块、函数、消息、异常栈；④ 关键链路日志：LLM 调用（记录输入输出 token 数、耗时、费用）、检索（记录 query、召回结果、耗时、相似度）、Agent 执行（记录 Plan、Execute、Reflect 每一步）；⑤ 便于问题排查和成本分析。

30. 怎么监控和告警？
A: 预留监控：① Prometheus 指标：QPS、延迟、错误率、LLM token 消耗、Embedding 耗时；② 告警规则：错误率 &gt; 5%、延迟 &gt; 3s、LLM 每日费用超阈值；③ 日志监控：用 ELK/Loki 收集，Grafana 可视化；④ 告警通知：钉钉/飞书/邮件。当前单机版，未来容器化部署加上。

31. RAG 板块做了哪些性能优化？
A: 优化点：① 异步全栈；② 多路召回并行执行（向量和 BM25 同时查）；③ 批量向量化；④ 缓存预留；⑤ 重排序只对 top N（如 20）做，不是所有召回结果；⑥ PostgreSQL 索引优化（倒排、元数据字段索引）。

32. 这个项目和 LangChain/LlamaIndex 做的 RAG 有什么不同？
A: ① 场景聚焦：专门针对会议，Speaker 模式优化，不是通用文档；② 工程化好：分层架构、异步、配置中心、日志监控完整，不是 demo；③ 评估体系全：自建数据集、RAGAS 集成、可回归；④ 轻量可控：没有过度抽象，代码好读好改；⑤ Agent 深度结合：不是只有问答，还有任务规划、工具调用。

33. 你在这个项目中最得意的技术点是什么？为什么？
A: （选一个深入讲，例如）最得意的是 Speaker 模式 + 短发言合并，从 59.6% Recall 提升到 86.1%，提升巨大。为什么：① 不是简单换模型，而是深入场景，找到痛点；② 规则 + 启发式，简单有效，cost 为 0；③ 数据驱动验证，有对比实验，效果明显；④ 体现了对业务场景的深度理解，而不是只会调参。

34. 这个项目最大的技术挑战是什么？你是怎么分析和解决的？
A: 最大挑战：碎片化发言导致 Recall 低。分析过程：看评估结果 → Speaker 模式 Recall 只有 59.6%，fixed 有 78% → 查 bad case → 发现很多短发言（"好的""对的""可以"）单独成 chunk → 这些太短，embedding 噪声大 → 检索不到。解决方案：短发言合并策略，同发言人连续短发言合并到阈值。验证：Recall 回到 86.1%。体现了：问题定位能力、数据分析能力、实验验证能力。

35. 这个项目中 RAG 板块的所有亮点总结？
A: ① 场景深度优化：Speaker 切分 + 短发言合并，针对会议场景；② 全链路 RAG：解析→切片→向量化→多路召回→重排序→生成，每个环节都有思考；③ 多路召回融合：向量+BM25，权重调优，取长补短；④ 完整评估体系：RAGAS + 自建数据集，数据驱动迭代；⑤ 异步工程化：生产级代码，不是 demo；⑥ 可演进架构：预留缓存、向量索引、知识图谱，支持未来扩展；⑦ 有量化效果：Recall 从 59.6%到 86.1%，相似度+7.5%，数据说话。

---

## Agent 决策与工具调用板块（共 20 问）

### 一、架构与工作流（7问）

36. 详细讲一下 Plan-Execute-Reflect 的工作流？每个阶段做什么？
A: ① **Plan阶段**：分析用户问题 → 拆解子任务列表 → 分析依赖关系（哪些可以并行，哪些必须顺序）→ 生成执行计划（JSON 格式）；② **Execute阶段**：按计划执行，无依赖并行，有依赖顺序，上下文传递（前序输出作为后序输入），调用工具；③ **Reflect阶段**：评估执行结果质量 → 检查是否完整、正确 → 如果有问题，是否需要 Re-Plan 或补充执行 → 汇总最终答案；④ **Checkpoint**：每步状态保存，支持断点续跑，错误重试。

37. 为什么选 LangGraph 而不是 ReAct 或其他？
A: 对比：① ReAct：简单，但循环不好控制，容易死循环，状态管理弱；② LangChain Agent：旧版不够灵活；③ LangGraph：显式状态机，边可以加条件判断，支持分支和循环，Checkpoint 机制完善，可观测性好，可控性强；④ 手写状态机：太麻烦，重复造轮子。选 LangGraph：平衡灵活性和可控性，生产可用。

38. Agent 的状态（State）是怎么设计的？包含哪些字段？
A: AgentState 定义：① user_query（用户原始问题）；② plan（生成的计划，任务列表）；③ task_results（每个任务的执行结果）；④ context（当前上下文，工具返回值）；⑤ final_answer（最终答案）；⑥ reflection（反思结果）；⑦ memory（记忆引用）；⑧ errors（错误列表）；⑨ step（当前步骤）。每一步更新状态。

39. 任务依赖分析是怎么做的？如何判断并行？
A: 当前基于规则+LLM：① 规则：看任务是否有前置依赖声明；② LLM 分析：输入问题和任务列表，输出依赖关系图；③ 优化：无相互依赖的任务放到同一组并行执行，提升速度；④ 例如："抽待办"和"查争议点"可以并行，"抽待办"和"发邮件通知"必须顺序。

40. 工具调用是怎么实现的？OpenAI Function Calling 格式兼容吗？
A: ① BaseTool 基类：定义 name、description、parameters_schema、execute()；② ToolRegistry：注册所有工具，按 name 查找；③ ToolExecutor：执行工具，错误处理；④ 兼容 OpenAI Function Calling 格式，可以直接给 LLM 看；⑤ 5个工具：search_meeting、extract_todos、generate_minutes、detect_controversies、answer_question。

41. 错误恢复机制详细讲一下？
A: 多层恢复：① **工具层**：工具执行失败返回 ToolResult(success=False, error=msg)，记录错误，继续其他任务；② **节点层**：AgentNodes 捕获异常，log，返回降级结果（如"该任务执行失败，忽略"）；③ **图层**：LangGraph 条件边，根据状态决定是否重试这个任务，还是跳过，或者走降级路径；④ **服务层**：AgentService 统一捕获，返回友好提示；⑤ **监控层**：Monitor 记录所有失败，用于分析和告警。

42. 计划验证和自动修复是怎么做的？
A: ① 语法验证：检查计划 JSON 格式是否正确；② 逻辑验证：检查任务是否有循环依赖，工具是否存在，参数是否齐全；③ LLM 验证：让 LLM 检查计划是否合理，有没有遗漏任务；④ 自动修复：简单问题自动修正（如参数缺失补默认值），复杂问题要求用户确认或重新 Plan；⑤ 提升整体成功率。

### 二、记忆与扩展（7问）

43. 记忆系统详细设计？短期、长期、实体关系？
A: ① **短期记忆 ShortTermMemory**：当前会话的对话轮次（question+answer），保留最近 10 条，LRU，用于多轮对话；② **长期记忆 LongTermMemory**：跨会话的关键事实、用户偏好，支持关键词索引检索，可过期；③ **实体关系 Entity/Relation**：从会议中抽取实体（PERSON、ORG、PROJECT）和关系（participate_in、responsible_for），未来知识图谱用；④ **MemorySystem**：统一管理以上；⑤ **Checkpoint**：LangGraph 自带的状态保存；当前进程内，未来 Redis/数据库持久化。

44. 记忆压缩是怎么考虑的？
A: 思路：① 保留最近 N 轮原始对话；② 更早的对话压缩成摘要（可以用 LLM 生成）；③ 关键事实提取出来存长期记忆；④ MemoryCompressor 类预留接口，当前简单拼接，未来可以 LLM 压缩；⑤ 避免 Context 窗口爆炸。

45. 人机确认（Human-in-the-loop）是怎么设计的？
A: 场景：① 敏感操作（删除会议、修改数据、发邮件）；② Agent 不确定时；③ 关键决策前。实现：① Agent 暂停，生成确认请求；② 存到内存/数据库；③ 提供 API 让用户确认或拒绝；④ 用户确认后继续执行，拒绝则终止或改计划；⑤ 增加安全性和可控性。

46. 任务模板（Prompt Templates）是怎么做的？怎么匹配？
A: ① 预设常见任务模板库（如"生成会议纪要"模板、"抽取待办"模板）；② 模板包含：name、description、trigger_keywords、prompt_template、required_tools；③ 用户问题来了，先关键词匹配，如果匹配到，直接用模板，不用 LLM 从零 Plan，速度快、省成本、稳定；④ 如果没有匹配到，再走 LLM Plan；⑤ 支持动态添加模板。

47. 流式输出的实现细节？Event 类型有哪些？
A: SSE 格式：event:xxx\ndata:yyy\n\n。Event 类型：① plan_start、plan_end（计划阶段）；② task_start、task_output、task_end（执行每个任务）；③ reflection_start、reflection_end（反思阶段）；④ token（生成最终答案的每个 token）；⑤ final_answer（完整答案）；⑥ error（错误）。前端对应展示，体验好。

48. 你考虑过多 Agent 协作吗？为什么当前单 Agent？
A: 考虑过，当前单 Agent 是因为：① 场景还没那么复杂；② 先把单 Agent 做好，再扩展。未来可以：① 按角色分工（Planner、Retriever、Generator、Critic）；② 按场景分工（会议Agent、待办Agent、搜索Agent）；③ 用 LangGraph 的 Subgraph 或 CrewAI/Autogen 等框架；④ Agent 通信通过共享 State 或消息队列。

49. Agent 的可观测性怎么做的？怎么调试？
A: ① **日志**：Plan、Execute、Reflect 每一步都打 log，记录输入输出、耗时、token 数；② **Trace**：给每个请求加 trace_id，全链路追踪；③ **可视化**：前端 AgentDemo.vue 实时展示执行过程，看 Plan 是怎么拆的，每个工具返回什么；④ **Checkpoint**：可以检查每个中间状态；⑤ **监控面板**：成功率、平均耗时、热门工具，预留接口。

### 三、亮点总结（6问）

50. Agent 死循环怎么防止？
A: ① 最大迭代次数限制 MAX_TASKS_IN_PLAN = 10；② Plan 验证阶段检查是否有循环；③ Reflect 阶段如果检测到重复步骤，停止；④ 超时控制；⑤ 降级路径，如果超过 N 步，直接返回简化答案。

51. 你对 Agent 的未来发展怎么看？
A: 前沿：① 多 Agent 协作（Multi-Agent System）；② Agentic RAG（Agent + RAG 深度结合，不是简单调用检索）；③ 更好的规划和推理（Tree of Thoughts、Graph of Thoughts）；④ 更小更快的 Agent（不需要那么大的模型）；⑤ 安全性和可控性（RLHF、Constitutional AI、Guardrails）；⑥ 工具生态标准化（OpenAI Tools、LangChain Tools 统一）。

52. 如果让你重新设计这个 Agent，你会怎么改进？
A: ① 记忆持久化（Redis/DB）；② 多 Agent 协作；③ 更强大的规划（Tree of Thoughts）；④ 用户反馈回路强化学习；⑤ 更多工具集成（日历、邮件、IM）；⑥ 更好的可观测性和调试工具；⑦ 安全 Guardrails（防止滥用）；⑧ 私有化部署方案。

53. Agent 板块和其他开源 Agent 框架比，有什么不同？
A: ① 场景化：专门为会议场景定制的工具；② 工程化：错误恢复、监控、日志都有；③ 轻量：没有过度抽象，代码好懂；④ Plan-Execute-Reflect 显式流程，比 ReAct 可控；⑤ 与 RAG 深度结合，不是两个独立模块。

54. 在 Agent 这块，你遇到的最大坑是什么？怎么解决的？
A: （举例）最大坑是 LLM 有时候会瞎编工具名或参数。解决：① 工具定义严格验证 schema；② ToolSelector 做检查，如果工具不存在，报错；③ 加 Plan 验证，LLM 自己检查计划中的工具和参数；④ 提供默认值和错误提示；⑤ 监控这类错误，优化 Prompt，给例子。

55. Agent 板块的所有亮点总结？
A: ① LangGraph 架构，Plan-Execute-Reflect 可控；② 5个会议专用工具，OpenAI 格式兼容；③ 三级记忆系统（短期/长期/实体），预留持久化；④ 完善错误恢复和降级；⑤ 任务模板，速度快省成本；⑥ Human-in-the-loop 预留，安全；⑦ 流式输出可视化，体验好；⑧ 全链路日志和可观测性。

---

## 系统架构与工程实现板块（共 18 问）

### 一、架构与选型（8问）

56. 整体架构图详细讲一下？各层职责？
A: 分层架构：① **前端层**：Vue 3 + Element Plus + Vite，页面：登录、会议列表/上传/详情、待办、文档、RAG 查询、Agent Demo、评估页面、配置页面；② **API 网关层**：FastAPI，路由 v1，中间件（CORS、访问日志、认证）；③ **服务层**：UserService、MeetingService、DocumentService、TodoService、RAGService、AgentService、LLMService、EmbeddingService、VectorSearchService、EvaluationService；④ **数据层**：SQLAlchemy 2.0 ORM，asyncpg 驱动 PostgreSQL，6 张表；⑤ **基础设施层**：Loguru 日志、Redis 缓存（预留）、Config 配置中心、Monitor 监控。

57. FastAPI 和 Flask/Django 对比？为什么选这个？
A: ① **异步**：FastAPI 原生 async/await，Flask 需要 gevent/eventlet，Django 虽然 3.1+ 支持但重；② **自动 API 文档**：OpenAPI/Swagger UI/ReDoc，开发调试爽；③ **类型安全**：Pydantic v2，请求响应验证，类型提示 IDE 支持；④ **依赖注入**：Depends() 很优雅，处理认证、数据库会话；⑤ **性能**：基于 Starlette + Uvicorn，ASGI，比 Flask 快；⑥ **生态**：好，和 LangChain/OpenAI 库兼容好。

58. PostgreSQL 和 MySQL 对比？为什么选它存向量？
A: ① **JSON/JSONB**：PostgreSQL 原生支持，存向量方便，MySQL JSON 功能弱；② **数组类型**：可以直接存 ARRAY[float]；③ **PGVector**：扩展可以做向量索引，未来可无缝升级；④ **全文搜索**：tsvector，比 MySQL 好；⑤ **事务和一致性**：PostgreSQL 更强；⑥ **开源生态**：SQLAlchemy/asyncpg 支持好。

59. 如果数据量变大，数据库怎么扩展？读写分离？分库分表？
A: ① **缓存**：Redis 挡热点查询，减轻 DB；② **读写分离**：主库写，从库读，检索可以走只读副本；③ **分表**：按会议ID/时间/租户分 vector_chunks 表（Range/List/Hash 分表）；④ **分库**：按业务域分库（用户库、会议库、向量库）；⑤ **向量库迁移**：10万级以上迁 Milvus/Qdrant，PostgreSQL 只存元数据；⑥ **连接池优化**：SQLAlchemy 连接池配置。

60. 怎么防止 SQL 注入？怎么处理并发写？
A: SQL注入防护：① 使用 ORM（SQLAlchemy），参数化查询，不要拼 SQL；② 输入验证（Pydantic）；③ 最小权限原则，DB 用户不给 drop/alter。并发写：① 数据库事务（ACID）；② 乐观锁（版本号）；③ 悲观锁（SELECT FOR UPDATE）；④ 队列串行化（高并发下先放队列，后台处理）；⑤ 当前量不大，事务足够，未来加队列。

61. 缓存策略是怎么考虑的？Cache-Aside？Write-Through？
A: 预留 Cache-Aside 模式：① 读：先查缓存，命中返回，未命中查 DB，回写缓存；② 写：先写 DB，再删缓存；③ 缓存 Key：前缀+业务+参数哈希；④ TTL：可配置，5分钟到1小时；⑤ 防雪崩：TTL 加随机值；⑥ 防击穿：热点 Key 互斥锁；⑦ 防穿透：空结果也缓存短时间。当前没启用，但代码有了。

62. 怎么保证代码质量？有单元测试/集成测试吗？
A: ① **单元测试**：pytest，测试 service 层的纯函数，mock 外部依赖（DB、LLM）；② **集成测试**：测试 API 端点，用 testclient，启动测试数据库；③ **静态检查**：mypy 类型检查，ruff/flake8  lint；④ **代码规范**：PEP 8，black 格式化，isort 排序 import；⑤ **CI/CD预留**：.github/workflows 可以加，自动跑测试、lint；⑥ 虽然是个人项目，但按生产标准写的。

63. 设计模式用了哪些？举例说明？
A: ① **依赖注入 Dependency Injection**：FastAPI Depends() 注入 db、current_user；② **工厂模式 Factory**：LLMServiceFactory、VectorSearchServiceFactory，根据配置创建实例；③ **策略模式 Strategy**：不同 ChunkingStrategy、不同 SearchStrategy，运行时切换；④ **装饰器模式 Decorator**：@cache 装饰器，@log 装饰器；⑤ **单一职责 Single Responsibility**：每个 Service 只做一件事；⑥ **开闭原则 Open/Closed**：Tool 可以加新的，不用改核心代码；⑦ **Repository 模式**：Repository 封装 DB 操作，Service 不直接碰 SQL。

### 二、安全与运维（6问）

64. 认证授权怎么做的？JWT 详细讲？
A: ① **认证**：登录接口，用户名/密码验证，密码 bcrypt 哈希存 DB，返回 access_token（JWT）；② **JWT 结构**：Header.Payload.Signature，Payload 存 user_id、exp（过期时间）；③ **授权**：依赖项 get_current_user，从 Header Authorization Bearer 取 token，验证签名和过期，获取 User 对象；④ **权限控制**：当前简单，用户只能看自己的会议，未来可以加 RBAC（角色权限）；⑤ **安全**：SECRET_KEY 存在环境变量，JWT 过期时间 ACCESS_TOKEN_EXPIRE_MINUTES=60。

65. 怎么防止 Prompt 注入？
A: 多层防护：① **输入过滤**：敏感词检测，长度限制；② **Prompt 加固**：分隔符清晰（如"""），明确告诉 LLM"用户输入不可信"；③ **输出验证**：检查输出是否包含敏感内容；④ **Principle of Least Privilege**：工具权限最小化；⑤ 用 LangChain 的 Guardrails 或 OpenAI Moderation API（预留）。

66. 敏感数据怎么处理？日志里有吗？
A: ① **数据脱敏**：日志中密码、token、手机号打码；② **最小化存储**：只存必要的，敏感字段加密存；③ **访问控制**：只有有权限的人能看；④ **审计日志**：谁、什么时候、访问了什么，记录下来；⑤ LLM 调用前检查：如果上下文有敏感信息，提示用户确认或过滤。

67. 怎么监控系统健康状态？
A: ① **健康检查接口**：GET /health，检查 DB、Redis（预留）、LLM 服务连通性；② **指标采集**：Prometheus，指标：QPS、P50/P95/P99 延迟、错误率、LLM 总 token、费用；③ **告警**：Alertmanager/Grafana Alerting，规则：错误率>5%、延迟>3s、日费用>阈值；④ **日志监控**：Loki + Grafana，检索错误日志；⑤ **链路追踪**：OpenTelemetry 预留。

68. 上线部署方案考虑过吗？容器化？K8s？
A: ① **容器化**：Dockerfile 写好，多阶段构建，优化镜像大小；② **Docker Compose**：本地一键启动（backend + db + redis）；③ **K8s 预留**：Deployment、Service、Ingress、ConfigMap、Secret、PVC 等 yaml 可以写；④ **CI/CD**：GitHub Actions，测试 → 构建 → 推送镜像 → 部署；⑤ **备份**：PostgreSQL 定期全量+增量备份到 OSS/S3；⑥ **监控**：Prometheus + Grafana。

69. 怎么处理百万级用户并发？
A: 架构演进：① **无状态 API**：可以水平扩展，多实例+负载均衡（Nginx）；② **缓存**：Redis 挡热点；③ **队列削峰**：Kafka/RabbitMQ，异步处理耗时操作（如批量向量化）；④ **数据库分库分表**：只读副本；⑤ **向量库**：专用分布式向量库；⑥ **多租户隔离**：Schema 或 Database 级别隔离；⑦ **限流降级**：：API Gateway 限流，降级服务。

### 三、亮点总结（4问）

70. 工程化方面做得最好的是什么？
A: ① 分层架构清晰，职责明确；② 全异步，性能好；③ 配置中心：pydantic-settings，环境隔离（dev/staging/prod）；④ 完善的日志和预留的监控告警；⑤ 类型安全，Pydantic v2；⑥ 单元测试和集成测试；⑦ 代码规范，lint + 格式化；⑧ 安全措施到位。

71. 如果让你重新设计架构，你会改什么？
A: ① 一开始就考虑用 PGVector，不用 JSON 存；② 加消息队列，异步处理批量任务；③ 前端用 React 而不是 Vue（个人偏好）；④ 更完善的多租户支持；⑤ 一开始就加监控告警面板；⑥ 预留 OpenTelemetry Trace。

72. 系统架构与工程实现板块的亮点总结？
A: ① 前后端分离，分层架构，职责清晰；② 全栈异步：FastAPI + asyncpg + AsyncOpenAI，I/O 密集型最优；③ PostgreSQL 多用：关系+向量+搜索，简化架构；④ 配置中心：pydantic-settings，灵活；⑤ 缓存预留：：Cache-Aside 模式，未来可加；⑥ 安全措施全：JWT、权限控制、输入验证、审计；⑦ 日志监控完整：：loguru，日志滚动，分级；⑧ 可部署性：：Docker 化，预留 K8s，CI/CD；⑨ 代码质量：：单元测试、lint、类型注解。

---

## 大模型调用与优化板块（共 15 问）

### 一、模型与 Prompt 工程（8问）

73. 为什么选 qwen3.6-plus？对比过 GPT-4、Claude、Llama 吗？
A: 对比分析：① **GPT-4**：效果最好，但贵，国内访问不稳定；② **Claude**：：效果不错，但也贵，国内访问问题；③ **Llama 3 本地**：：效果可以，但需要 GPU，部署成本高，速度慢；④ **Qwen-Plus**：：阿里云通义千问，效果接近 GPT-4，便宜很多，国内访问稳定，OpenAI 兼容接口，迁移成本低；⑤ **Qwen-Turbo**：：评估用，轻量快速，省钱。

74. 温度参数（Temperature）怎么选？为什么 0.7？
A: 温度控制随机性：① 0.0-0.3：确定、重复，适合事实问答；② 0.5-0.8：平衡，适合会议纪要生成；③ 0.9-1.0：创意，适合头脑风暴。选 0.7：平衡答案质量和多样性。不同任务可调：问答 0.7，生成纪要 0.9，抽待办 0.2。

75. 详细讲一下你的 Prompt 工程？System Prompt 怎么写的？
A: ① **双层结构**：System Prompt + User Prompt；② **System Prompt**：设定角色（"会议智能助手"）、设定约束（"不知道就说不知道"、"不要编造"、"简洁明了"）、指令；③ **User Prompt**：用户问题 + 参考信息（标注来源，如"[1] 张三说：..."）；④ **Few-Shot**：给例子（针对难任务）；⑤ **Chain-of-Thought**：推理型问题让 LLM 先想后答；⑥ **自适应**：根据 Query 类型选不同 System Prompt（事实型、总结型、对比型、推理型）。

76. 什么是 Context Window？怎么用满但不超限？
A: qwen3.6-plus 是 32k（或 128k）Context Window。策略：① 先算 token 数（或字符数）；② 按 similarity 排序，从高到低加；③ 留 buffer 给 Prompt 模板和生成答案；④ 最后一个 chunk 可截断但至少留 100；⑤ 超出丢弃；⑥ 预留压缩接口。当前是字符数简单估算，未来可以用 tiktoken 或 qwen tokenizer。

77. 怎么优化 LLM 调用的速度和成本？
A: 速度：① 异步调用；② 并行调用；③ 流式输出（首字快）；④ 选合适的模型（小任务用 qwen-turbo）；⑤ 缓存常见问题；⑥ 减少 max_tokens。成本：① 模型选择（Turbo 比 Plus 便宜）；② max_tokens 限制；③ Context 压缩；④ 缓存命中；⑤ 批量调用；⑥ 监控费用，设置预警。

78. 怎么让 LLM 稳定输出 JSON？
A: 方法：① **Prompt 明确要求**："以 JSON 格式输出，不要其他内容"，给 schema 示例；② **正则提取**：即使有 markdown 或其他内容，正则抓出 {...}；③ **OpenAI JSON Mode**：response_format={"type": "json_object"}（qwen 也支持）；④ **Pydantic 验证**：输出后用 Pydantic 解析，失败重试；⑤ **Few-Shot**：给几个 JSON 输出例子。

79. 有做过 Prompt 版本管理和 A/B 测试吗？
A: 当前没线上，但预留架构：① **Prompt 模板存 DB/配置文件**：有版本号；② **A/B 测试**：不同用户组看到不同 Prompt；③ **指标**：Answer Similarity、用户满意度、完成率；④ **统计显著性**：t-test。未来可以加 Prompt Market 界面，方便管理和测试。

80. 你对 Prompt Engineering 的前沿发展怎么看？
A: 前沿：① **自动 Prompt 优化**：AutoPrompt、APE、DSPy（自动优化 Prompt，不是靠人工调）；② **结构化 Prompt**：LangChain PromptTemplate，Pydantic 绑定；③ **Agentic Prompting**：让 LLM 自己规划怎么写 Prompt；④ **Prompt Chaining**：分多步，第一步生成子问题，第二步查，第三步答；⑤ **少样本/零样本改进**：Chain-of-Thought、Tree-of-Thought、Self-Consistency；⑥ 未来可能 Prompt 越来越不重要，模型能力越来越强，但现在还是很重要。

### 二、容错与亮点总结（7问）

81. LLM API 调用失败、超时怎么处理？详细讲降级策略？
A: 多层降级：① **超时控制**：设置合理 timeout（默认 20s）；② **重试机制**：指数退避重试 2-3 次（针对网络抖动）；③ **模型降级**：Plus 失败自动切 Turbo 或其他备用模型；④ **输出降级**：流式失败转非流式；⑤ **检索兜底**：LLM 完全不可用时，只返回检索到的 chunks 列表，让用户自己看；⑥ **友好提示**：告诉用户"服务繁忙，稍后再试"或"返回部分结果"；⑦ **监控告警**：记录失败次数和原因。

82. 怎么统计和控制 LLM 成本？
A: ① **监控**：每次调用记录 input tokens、output tokens、model、price，统计日/周/月费用；② **预警**：设置费用阈值，快到了发通知；③ **限制**：设置单用户/单请求 max_tokens，防止滥用；④ **优化**：Context 压缩，小任务用小模型，缓存；⑤ **账单**：定期看阿里云/OpenAI 账单，对比自己的统计，防止超。

83. 有考虑过本地部署大模型吗？比如 Qwen-7B？
A: 考虑过，方案：① **Embedding**：bge-m3 可以本地部署，用 sentence-transformers 或 fastembed；② **LLM**：：Qwen-7B/14B 量化（GPTQ/AWQ）后可以在消费级 GPU 跑，用 vLLM/Text-Generation-Inference 加速；③ **RAGAS 评估**：小模型也可以做，慢点但省钱；④ **优势**：数据不出境，隐私安全，长期成本低；⑤ **劣势**：部署运维复杂，效果比 qwen3.6-plus 差，速度慢；⑥ 预留接口：配置里有 LOCAL_MODELS_ROOT，可以一键切换。

84. Function Calling（工具调用）是怎么实现的？怎么保证格式正确？
A: ① **工具定义**：继承 BaseTool，返回 ToolDefinition（包含 name、description、parameters：OpenAI Schema 格式）；② **传给 LLM**：tools 参数给 LLM；③ **解析 LLM 输出**：看 LLM 返回的是 tool_calls 还是直接回答；④ **执行工具**：ToolExecutor 执行，获取结果；⑤ **回传给 LLM**：tool result 作为下一轮上下文，让 LLM 继续；⑥ **验证**：工具名、参数用 Pydantic 验证，错误有提示。

85. 流式输出的异步生成器怎么实现的？
A: FastAPI StreamingResponse：① 定义一个 async def generate() 函数；② LLMService.chat(stream=True) 返回 async generator；③ async for token in generator: yield f"data: {token}\n\n"；④ 前端用 EventSource 或 fetch + ReadableStream 接收；⑤ 同时可以推送非 token 事件（plan、task 等）。

86. 大模型调用这块遇到的最大坑是什么？怎么解决的？
A: （举例）最大坑是 LLM 输出不稳定，有时候符合格式，有时候不符合。解决：① 加明确的 Prompt 约束和 Few-Shot 例子；② 用 OpenAI JSON Mode（qwen 支持）；③ 加 Pydantic 验证+正则提取兜底；④ 失败重试 2 次；⑤ 还不行就降级返回默认格式；⑥ 监控这类错误，持续优化 Prompt。

87. 大模型调用与优化板块的亮点总结？
A: ① 模型选型合理：性价比高，国内稳定；② Prompt 工程完善：自适应、Few-Shot、CoT、双层结构；③ 多级容错：超时、重试、降级、兜底；④ 成本控制：监控、限制、优化措施；⑤ 流式输出体验好：SSE + 前端展示；⑥ 函数调用规范：OpenAI 格式兼容，验证完善；⑦ 本地部署预留：灵活切换云/本地；⑧ 费用监控：心里有数，防超支。

---

## 评估与迭代板块（共 12 问）

### 一、评估体系与指标（7问）

88. 详细讲一下 RAGAS 的 6 个指标？每个怎么算？
A: RAGAS（Retrieval-Augmented Generation Assessment）：
① **Faithfulness 忠实度**：Answer 是否完全来自 Context，有没有编造；用 LLM 评分 0-1；
② **Answer Relevance 答案相关性**：Answer 和 Question 是否相关；用 LLM 或 Embedding 相似度；
③ **Context Precision 上下文精度**：Retrieved Context 中相关的占比（前 K 个中的正确数）；
④ **Context Recall 上下文召回**：Ground Truth 需要的信息在 Retrieved Context 中的覆盖度；
⑤ **Answer Similarity 答案相似度**：生成 Answer 和 Ground Truth Answer 的语义相似度（bge-m3 算）；
⑥ **Answer Correctness 答案正确性**：综合评分，人工或 LLM；
我们实现了全 6 个，快速模式可以只算检索指标（Recall@K、Precision@K、MRR）。

89. 你的评估数据集是怎么构建的？233正例+30负例具体怎么来的？
A: 流程：① **需求定义**：覆盖事实型、推理型、总结型、对比型问题；覆盖不同会议类型；包含正例和负例；② **人工设计种子问题**：先写 20-30 个高质量问题和标准答案；③ **LLM 批量生成**：Few-Shot + 示例，让 LLM 生成更多；④ **人工筛选和修正**：检查问题质量，修正标准答案；⑤ **加入干扰文档**：22% 无关文档（旅游、新闻），测试鲁棒性；⑥ **保存为 JSONL/CSV**：方便加载；⑦ 虽然半自动，但质量可控，成本低。

90. Recall@5、Precision@5、MRR 具体怎么定义？你的数值是多少？
A: ① **Recall@5**：Ground Truth 的相关 chunk 在 Top 5 检索结果里的比例；越高越好；
② **Precision@5**：Top 5 检索结果里相关 chunk 的比例；越高越好；
③ **MRR (Mean Reciprocal Rank)**：第一个正确结果的排名的倒数的平均值；例如：正确答案排第 1，贡献 1/1；排第 2，贡献 1/2；平均所有 Query；衡量排序质量；
我们的数值（优化后）：Recall@5: ~85.6%，Precision@5: ~73%，MRR: ~0.75，Answer Similarity: ~0.79。

91. 怎么评估负例问题？拒答准确率怎么算？
A: 负例是答案不在知识库的问题。评估：① 看系统是否说"没找到"或"无法回答"；② 如果编造了答案，算错；③ **拒答准确率** = 正确拒答的负例数 / 总负例数；我们当前人工抽检拒答准确率约 80%；未来可以训练专门的分类器，或者用 LLM 打分判断"是否应该拒答"。

92. 怎么设计 A/B 测试来对比不同版本？
A: A/B 测试设计：① **随机分流**：用户/请求随机分到 A 组（旧版本）或 B 组（新版本）；② **指标**：线上：用户点赞率、完成率、Session 时长；离线：Recall、Precision、MRR、Answer Similarity；③ **统计显著性**：t-test 或卡方检验，p-value < 0.05 认为有显著差异；④ **置信区间**：95%；⑤ **持续运行**：直到样本量足够；⑥ **决策**：如果 B 组指标显著好，全量切换；否则回滚或继续优化；我们当前是离线评估，预留了线上 A/B 框架。

93. 评估结果怎么指导优化？举个例子？
A: 例子 1：MRR 低 → 说明排序不好 → 优化重排序模型或调整 BM25/向量权重；例子 2：Faithfulness 低 → 幻觉多 → 优化 Prompt，加更多约束，或者 Context 压缩去掉不相关的；例子 3：某类问题 Recall 低 → 检查这类问题的 Query 改写是否有效，或者切分策略是否合理；例子 4：负例拒答差 → 加相似度阈值，或者专门的拒答分类器。用数据驱动，不是拍脑袋。

94. 人工评估怎么设计？和自动评估怎么结合？
A: ① **人工评估**：抽样 50-100 个问题，让 2-3 人标注重复度、相关性、正确性，计算 Cohen's kappa；② **混合评估**：自动指标先过一遍（快、可扩展），人工只看边界 case（慢但准）；③ **Bad Case 分析**：人工看自动评估低分的例子，分析原因；④ **持续迭代**：人工发现的 pattern 可以加到自动评估里（如新加规则、优化 Prompt）。

### 二、迭代与亮点总结（5问）

95. 你做过哪些具体的迭代优化？每个迭代的效果数据？
A: 迭代历史：
① **v0.1**：纯向量检索，fixed 切分，MiniLM：Recall@5 ~70%，Answer Similarity ~0.70；
② **v0.2**：换 bge-m3：Recall +8.6% → 78.6%，Similarity +2.5% → 0.725；
③ **v0.3**：Speaker 模式：Recall 掉到 59.6%，但更符合场景；
④ **v0.4**：加短发言合并：Recall 恢复到 86.1%；
⑤ **v0.5**：加 BM25 融合：命中率 +5%；
⑥ **v0.6**：加重排序：Answer Similarity +3% → 0.79；
每次迭代都有对比数据，用评估数据集验证。

96. 怎么处理 Bad Case？流程是怎样的？
A: Bad Case 处理流程：① **收集**：从评估结果、用户反馈、日志中收集；② **分类**：是检索问题（没召回正确 chunk）？还是生成问题（召回了但没答对）？还是切分问题？还是 Prompt 问题？③ **根因分析**：看日志、看 chunk、看 Prompt，找到为什么；④ **优化**：针对根因改（调整切分、换 embedding、优化 Prompt、加重排序等）；⑤ **验证**：用评估数据集或 Bad Case 单独测试，看是否修复；⑥ **回归**：确保没把好的改坏；⑦ **沉淀**：把这个 Bad Case 加入测试集，防止回归。

97. 怎么用用户反馈来优化系统？
A: 用户反馈回路：① **收集**：点赞/点踩、用户修正答案、用户输入新的正确信息；② **关联**：反馈关联到 Query、Chunks、Answer；③ **分析**：统计高频 bad case 类型；④ **优化**：用反馈 fine-tune 重排序模型、用 Hard Negative 训练检索、优化 Prompt、补充知识库；⑤ **实验**：A/B 测试新策略；⑥ **关闭回路**：新版本上线，继续收集反馈，继续优化。当前预留接口，未来接上。

98. 评估与迭代这块的亮点总结？
A: ① **RAGAS 集成**：6 大指标全面评估；② **快速/深度双模式**：日常调参用快速，发布前用深度；③ **自建数据集**：233正+30负，22%干扰文档，覆盖会议场景；④ **数据驱动迭代**：每个优化都有对比数据；⑤ **Bad Case 流程**：根因分析、验证、回归、沉淀；⑥ **A/B 测试预留**：未来可线上；⑦ **用户反馈预留**：可闭环优化；⑧ **可视化页面**：EvaluationPage.vue 方便看结果。

---

## 项目经验与个人总结板块（共 10 问）

99. 这个项目如果在大厂落地，你觉得还要做什么？
A: 大厂落地 checklist：① **基础设施**：容器化、K8s 部署、CI/CD、可观测性全链路；② **数据**：大规模真实数据测试，数据质量清洗，隐私合规（GDPR/等保）；③ **安全**：更严格的认证授权、审计日志、数据加密、安全扫描；④ **性能**：压测（JMeter/Locust）、性能调优、缓存优化、数据库优化；⑤ **稳定性**：多 AZ 部署、容灾、降级、限流、熔断（Sentinel/Resilience4j）；⑥ **成本**：成本优化（模型选择、缓存、压缩）、ROI 分析；⑦ **产品**：完整的产品功能，用户体验，运营后台；⑧ **团队**：多人协作，代码 review，文档完善，OnCall 机制。

100. 从这个项目中，你学到的最重要的 3 个技术点是什么？
A: ① **RAG 全链路理解**：不是只有向量检索，切分、召回、重排序、Prompt 每个环节都很重要；② **工程化能力**：异步、缓存、日志、监控、分层架构这些比算法本身更影响落地；③ **数据驱动迭代**：没有评估体系就不知道效果好不好，瞎优化没用，评估体系是基础设施。

101. 如果让你重新从零开始做这个项目，你会怎么规划？
A: 规划：① **Week 1**：需求调研，竞品分析，技术选型；② **Week 2**：搭建基础架构（FastAPI+DB+前端），用户会议待办 CRUD；③ **Week 3**：基础 RAG（解析切片向量化检索生成）；④ **Week 4**：评估体系，数据集，优化 RAG（多路召回+重排序）；⑤ **Week 5**：Agent 系统（LangGraph+工具）；⑥ **Week 6**：工程化（日志监控缓存部署）；⑦ **Week 7**：测试、Bugfix、文档、演示准备；⑧ **持续迭代**：根据反馈优化。

102. 你在这个项目中担任什么角色？主导/协作？
A: （根据实际调整，例如）：我是独立完成的，全栈开发：后端 FastAPI、前端 Vue、RAG 系统、Agent 系统、数据库设计、评估体系、文档。但在大厂会是团队：算法工程师（RAG/Agent）、后端、前端、运维、产品。我相当于一个人扮演了多个角色，锻炼了全栈能力。

103. 这个项目的不足和遗憾是什么？未来怎么改进？
A: 不足与遗憾：① 前端不够完善，部分页面是 demo 级；② Agent 记忆没有持久化；③ 知识图谱模块没有和主检索链路打通；④ 没有真实用户反馈，全是离线评估；⑤ 没有线上 A/B 测试；⑥ 没有处理多模态（会议录音/视频）；改进计划：前面板块都说了。诚实承认不足，但也说明改进思路，体现思考。

104. 你对 AI 应用开发的理解是什么？和传统开发有什么不同？
A: 不同点：① **非确定性**：LLM 输出不稳定，传统是确定的，所以需要更多容错、降级、验证；② **数据驱动**：评估体系和数据比代码重要，持续迭代；③ **Prompt 工程**：不是只有代码，还要会写 Prompt；④ **成本敏感**：Token 就是钱，每一步都要算；⑤ **可观测性**：更重要，因为 LLM 是黑盒；⑥ **快速迭代**：模型更新快，应用也要跟着变；相同点：工程化、架构、分层、测试这些还是一样重要。

105. 你觉得这个项目能体现你的什么能力？
A: ① **工程化能力**：全栈开发、分层架构、异步、缓存、日志监控，生产级代码；② **算法理解与应用**：RAG、Embedding、重排序、LangGraph Agent，不是只会调包，理解原理；③ **问题解决能力**：Bad Case 分析、数据驱动优化；④ **学习能力**：快速学习 LangGraph、RAGAS、bge 等新技术；⑤ **项目管理能力**：独立规划和完成一个完整项目；⑥ **代码质量意识**：类型注解、测试、lint、规范。

106. 面试时展示这个项目，你觉得最好的方式是什么？
A: 展示策略：① **1分钟 Elevator Pitch**：一句话说清楚项目；② **架构图展示**：5分钟讲架构和技术选型；③ **效果数据展示**：3分钟讲优化过程和指标；④ **Demo 演示**：5分钟：RAG 问答（好的和 Bad Case 对比）、Agent 任务规划流式执行；⑤ **技术亮点深挖**：面试官感兴趣哪个就深入讲哪个；⑥ **故事化**：讲一个 Bad Case 分析解决的故事，比纯技术生动；⑦ **不要读稿**：要理解，能讲清楚，能回答追问；⑧ **材料准备**：：一页架构、一页数据、一页亮点、演示环境。

107. 这个项目中，你最有成就感的是什么？
A: （根据实际回答，例如）最有成就感的是 Speaker 短发言合并那个优化，看到 Recall 从 59.6% 升到 86.1%，提升巨大，非常有成就感。不是靠堆模型，而是靠深入分析场景、找到痛点、简单有效的方案解决问题，而且数据验证了效果，不是自嗨。这说明我不是只会调参的调包侠，而是会思考、会分析、会验证的工程师。

108. 项目经验与个人总结板块的亮点总结？
A: ① **项目完整度高**：从后端到前端，从 RAG 到 Agent，从评估到工程化，全链路覆盖；② **有量化结果**：不是空泛说"效果好"，有 Recall 从 59.6% 到 86.1% 这种具体数据；③ **有迭代过程**：不是一步到位，有多个版本，每个版本有对比，体现数据驱动；④ **有 Bad Case 故事**：体现分析解决问题能力；⑤ **有思考深度**：对不足、未来、大厂落地都有想法；⑥ **技术全面**：算法+工程+产品思维都有；⑦ **展示策略清晰**：知道面试时怎么讲。

---

## 综合面试准备：技术深度拓展篇

### 大厂常考知识点（RAG & Agent & 架构）

#### RAG 进阶
1. **检索增强的其他方法**：HyDE、Parent-Child Chunking、ColBERTv2、SPLADE；
2. **向量索引算法**：HNSW（图索引）、IVF（聚类倒排）、FAISS；
3. **RAG 微调**：RAFT（Retrieval-Augmented Fine-Tuning）、LoRA、QLoRA；
4. **多模态 RAG**：图片用 CLIP 或 Qwen-VL，音频用 Whisper 转文本；

#### Agent 进阶
1. **Agent 框架对比**：LangGraph、LlamaIndex Workflow、CrewAI、AutoGen；
2. **规划方法**：ReAct、Plan-and-Solve、Tree-of-Thoughts、Reasoning without Observation；
3. **多 Agent 通信**：共享内存、消息队列、黑板模式；

#### 架构与工程化
1. **分布式系统**：CAP、BASE、一致性哈希、负载均衡；
2. **数据库**：索引原理、事务隔离级别、乐观锁悲观锁；
3. **缓存**：Redis 数据结构、缓存穿透/击穿/雪崩、分布式锁；
4. **消息队列**：Kafka/RabbitMQ 对比，削峰填谷，异步解耦；
5. **容器与编排**：Docker、K8s 核心概念；
6. **可观测性**：Logging、Metrics、Tracing（OpenTelemetry）；

### 面试回答套路
1. **STAR 原则**：讲项目故事时用（Situation 背景、Task 任务、Action 行动、Result 结果）；
2. **先总后分**：先一句话概括，再展开细节；
3. **数据说话**：不要说"优化了性能"，要说"QPS 从 10 到 50，延迟从 1s 到 200ms"；
4. **诚实面对不足**：不会就说不会，但可以说"我会怎么去学习/解决"；
5. **引导话题**：把面试官往你准备过的亮点上引。

祝面试顺利，拿到大厂 SP！
