[pytest]
testpaths = tests/contracts tests/agent tests/api tests/chunking
asyncio_mode = auto
python_files = test_*.py
python_classes = Test*
python_functions = test_*
addopts = --tb=short

# 默认门禁只运行不依赖外部服务的主链测试。
# PostgreSQL、Redis、RabbitMQ 集成测试由 CI 的 infrastructure-integration job 显式运行。
