# ============================================
# MeetingMind - 会议智能 Agent 系统依赖
# 基于 conda meetingmind 环境导出
# ============================================

# 核心框架
fastapi==0.115.0
uvicorn[standard]==0.30.6
sqlalchemy==2.0.35
alembic==1.13.3
asyncpg==0.29.0
pydantic==2.9.2
pydantic-settings==2.5.2
pydantic[email]==2.9.2
python-multipart==0.0.12
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
loguru==0.7.2
python-dotenv==1.0.1
httpx==0.27.2
email-validator==2.3.0
greenlet==3.1.1
redis[hiredis]==5.0.8
jieba==0.42.1
werkzeug>=3.0,<4.0
jsonschema>=4.20,<5.0

# 缓存相关
fastapi-cache2==0.2.2

# LangChain 生态系统锁定
langchain-core==0.2.43
langgraph==0.2.76
langgraph-checkpoint==2.1.2

# ============================================
# 向量与检索（CPU版本，适合Docker部署）
# ============================================
sentence-transformers==2.2.2
faiss-cpu==1.14.3
numpy>=1.24.3
scipy>=1.13.1

# PyTorch（CPU版本，Docker部署使用）
torch --index-url https://download.pytorch.org/whl/cpu
torchvision --index-url https://download.pytorch.org/whl/cpu

# Transformers（FlagEmbedding依赖）
transformers>=4.44.0
accelerate>=0.20.0
safetensors>=0.4.0

# LLM 接口
openai>=1.0.0

# 文档解析
pdfplumber==0.11.4
pypdf==5.1.0
python-docx==1.1.2
openpyxl==3.1.5

# 实验与评估依赖
tqdm>=4.66.0
scikit-learn>=1.0.0
ragas==0.2.11

# FlagEmbedding (BGE-M3 + Reranker)
FlagEmbedding==1.4.0

# 测试依赖
pytest==8.3.3
pytest-asyncio==0.23.6
pytest-cov==5.0.0

# 异步支持
aiofiles>=23.2.0
aio-pika>=9.4.0

# ============================================
# 知识图谱
# ============================================
neo4j>=5.0.0

# ============================================
# DSPy（提示优化）
# ============================================
dspy-ai>=2.0.0

# ============================================
# 监控与可观测性
# ============================================
prometheus-fastapi-instrumentator==6.1.0
prometheus_client==0.20.0

# ============================================
# MCP (Model Context Protocol)
# ============================================
fastmcp>=0.1.0

# ============================================
# 可观测性（OpenTelemetry）
# ============================================
opentelemetry-sdk>=1.20.0
opentelemetry-api>=1.20.0
opentelemetry-instrumentation-fastapi>=0.46b0
opentelemetry-exporter-otlp-proto-grpc>=1.20.0
opentelemetry-exporter-otlp-proto-http>=1.20.0
opentelemetry-semantic-conventions>=0.46b0

# ============================================
# 性能测试
# ============================================
locust>=2.0.0

# ============================================
# 可选依赖（GPU环境使用）
# ============================================
# 以下依赖在有GPU的环境中安装：
# bitsandbytes>=0.41.0  # 量化支持
# peft>=0.4.0  # LoRA微调
# vllm>=0.5.0  # 推理优化
# torch --index-url https://download.pytorch.org/whl/cu121  # GPU版本
